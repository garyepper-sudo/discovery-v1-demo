# Sprint 78 — Commit 2

## Goal

Replace hardcoded product organization IDs with the canonical organization resolver.

---

## Background

The Runtime already supports multiple organizations.

A canonical helper now exists:

```
engine/v3/runtime/activeOrganization.ts
```

It exports:

- DEFAULT_ORGANIZATION_ID
- resolveOrganizationId()

All production application code should resolve organization identity through this helper.

Do not redesign Runtime.

---

## Scope

Modify ONLY:

- app/api/executive-decision/route.ts
- app/api/executive-decision-record/route.ts
- app/api/executive-scenario/route.ts
- app/api/discovery-lab/route.ts

---

## Requirements

1. Import resolveOrganizationId().
2. Replace hardcoded product organization IDs.
3. Discovery Lab must no longer use `body.company` as organization identity.
4. Preserve request and response contracts.
5. Preserve validation behavior.

---

## Out of Scope

Do NOT modify:

- engine/benchmark/**
- Runtime
- React components
- Onboarding
- Clerk
- Documentation

---

## Validation

Run:

```
npm run typecheck
npm run build
```

---

## Deliverables

Report:

- files changed
- summary
- validation
- concerns

Do not commit.

Do not push.