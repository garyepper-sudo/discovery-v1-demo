# AR-2 Critical-Path Test Pyramid Governance Inventory

- Contract: `AR-2-CRITICAL-PATH-TEST-PYRAMID-FREEZE-001`
- Title: `AR-2 — CRITICAL-PATH TEST PYRAMID FREEZE`
- Canonical parent: `20619629c3330aabbd3cb63a05ddf1ce44fccca0`
- Inventory schema: `1`
- Inventory digest: `40bfc9de7e27db5107b2bf275c59784e8aac66bd58b2076e958509ca69fd84fb`
- Governance classification: requirements and mappings only; no AR-2 execution result

## Purpose

Freeze the smallest complete, owner-aligned, deterministic test pyramid proving the Alpha consequential Product path and launch-critical invariants. AR-2 is a readiness/test-system freeze, not Product implementation, a new testing framework, benchmark execution, or production operations.

## Pyramid

| Layer | Frozen meaning |
|---|---|
| L0 — static and contract firewall | Type, build, schema, import, dependency, source, architecture, and governance integrity; no Product fact |
| L1 — canonical owner deterministic validation | Direct typed execution of existing semantic owners |
| L2 — cross-owner composition and integration | Product/Workflow composition, lifecycle transitions, lineage, and joined correspondence |
| L3 — replay, restart, recovery, and fresh process | Replay, CAS, failure recovery, restart, canonical-byte preservation, duplicate/foreign safety |
| L4 — authenticated Product journey | Intentionally small real user-visible journey through frozen Foundation V1.2 owners |
| L5 — integrated readiness and evidence closure | Qualified observations, PRE-001A adjudication, scanner, cleanup, source binding, verification, and preservation |

No critical invariant may rely only on L4 when a lower owner-aligned proof is practical. L5 aggregates qualified observations and never manufactures owner facts.

## Frozen inventory

- Critical paths: **31**
- Inline critical-path owner assignments: **31**
- Distinct normalized owner path/symbol bindings: **18**
- Unsupported critical invariants: **0**
- Duplicate or conflicting semantic owner mappings: **0**
- Negative controls: **26**

The inventory covers tenant confinement; authorization-before-load and neutral non-disclosure; Product Question/series/occurrence/operation/receipt identity; replay/CAS; Private Working and intentional contribution including empty contribution; Freeze, Capture, Review, closure, What Changed, Prepare Again, and successor lineage; Occurrence-2 prepared/not-started truth; reload/fresh-process reconstruction; Runtime/Workflow recovery; materialization; joined inventories; missing/duplicate/foreign safety; AR-3 and AR-4 neutrality; authenticated roles/viewports; partial provisioning; two-pass cleanup; fresh independent zero; scanner containment; source-bound adjudication; and repository validation.

The complete semantic definitions, exact owner paths/symbols, required layers, commands, and negative controls are canonical in the JSON inventory.

## Canonical command lanes

| Lane | Count | Purpose |
|---|---:|---|
| Fast | 12 | 11 admissible L0–L2 firewall/owner checks plus one diagnostic-only materialization wrapper |
| Recovery | 9 | L2–L3 replay, materialization, recovery, parity, and provenance checks |
| Authenticated | 1 | Exact final-candidate and canonical-integrated Foundation V1.2 Product journey |
| Closure | 2 | Planned AR-2 write/verify commands owned by the later bounded aggregator |

Existing commands are reused as-is. Execution layer and evidence layers carried are distinct: static firewalls execute and prove only L0, while L5 closure commands carry already-qualified L0–L4 evidence without manufacturing it. The two closure commands are explicitly planned additions in the later implementation maximum; no command is silently assumed. Historical or overlapping validators remain diagnostics/regressions unless the JSON command inventory names them as canonical.

## Frozen thresholds

- Mandatory commands and required critical facts: **100%**.
- Unsupported, conflicting, static/transformed/inferred, FAIL, BLOCKED, unauthorized-read, duplicate-record, scanner-finding, residue, new-architecture-finding, and package-lock-drift totals: **0**.
- Evidence-producing commands: **2** consecutive normalized-identical runs.
- Existing architecture baseline: exactly **7 inherited findings**, with zero new findings.
- Any timing luck, arbitrary retry, timeout broadening, execution-order dependence, ambient-state dependence, prior residue, manual browser intervention, or inferred cleanup is `BLOCKED — NONDETERMINISTIC TEST PYRAMID`.
- Overall precedence: `BLOCKED > FAIL > PASS`.

## Later implementation maximum

Architecture classification: **contracts-plus-aggregator with one bounded
authenticated owner adapter**. PRE-001A remains sufficient for authenticated
envelopes, but it cannot represent AR-2 layer, command, coverage,
deterministic-repeat, source/inventory, and overall-pyramid results. One
AR-2-specific contracts path therefore owns only those closed noncanonical
schemas and pure validation.

`AR-2 FUTURE IMPLEMENTATION MAXIMUM — EXACTLY 6 PATHS`

1. `package.json` — three exact AR-2 authenticated/write/verify commands only.
2. `scripts/alpha-readiness/criticalPathTestPyramidContracts.ts` — closed AR-2 layer, command, coverage, determinism, source-binding, evidence, and result schemas plus pure validation.
3. `scripts/alpha-readiness/runCriticalPathTestPyramid.ts` — bounded inventory loader, orchestrator, typed admission, ordering/frequency/repeat enforcement, evidence writer/verifier, scanner, cleanup gate, and Control Tower verifier; no semantic-owner logic.
4. `scripts/acceptance/validateAr5bAuthenticatedRecoveryConformance.ts` — one import-safe typed current-build measurement API preserving historical CLI/evidence semantics.
5. `docs/agent-work-orders/evidence/alpha-readiness/ar2/AR2_CRITICAL_PATH_TEST_PYRAMID_RESULTS.json` — machine evidence.
6. `docs/agent-work-orders/evidence/alpha-readiness/ar2/AR2_CRITICAL_PATH_TEST_PYRAMID_REPORT.md` — deterministic JSON-derived report.

The separate Control Tower path is `docs/Product/PRODUCT_ROADMAP.md`; it is not
an implementation path. Conditional paths: **none**. No hidden seventh
implementation path is presently known. Product/Foundation/completed-gate
changes, dependencies/lock changes, another owner adapter, or
production/customer authority are stopping conditions.

AR-2 must rerun the authenticated Foundation V1.2 lane because its package and
new source bytes change the complete current-build identity bound by AR-5B.
Historical AR-5B evidence remains immutable provenance and cannot supply that
new fact. The exact future command is
`npm run validate:alpha-readiness-ar2:authenticated`; it invokes the bounded
AR-5B observation-only adapter, uses one owner-issued finite development/test
task plan, cleans twice, and requires distinct fresh task-scoped zero.

## Evidence and cleanup model

The later JSON binds contract, parent, inventory/layer/command/owner/threshold digests, complete source manifest, source identity, per-layer and per-path results, deterministic repeat, scanner, cleanup, independent zero, architecture, package lock, and overall result. Markdown derives solely from JSON. Static PASS, declared totals, historical relabeling, exit-only owner proof, manual coverage, and silent not-applicable outcomes are rejected.

Every process/external command retains its existing owner cleanup contract. The authenticated lane requires normal cleanup attempt one, idempotent attempt two, fresh independent task-scoped zero, and foreign/ambiguous-resource preservation. Emergency remediation invalidates that execution.

## Unresolved items

None at governance-definition time. AR-2 execution remains **not begun** and requires a separate Sensitive Gate Contract against this exact canonical inventory.
