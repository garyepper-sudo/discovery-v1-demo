# Canonical Understanding Audience-Lineage Foundation 001

**Status:** Corrected shadow-only foundation; incomplete
**Gap:** `GAP-B-014`
**Roadmap:** Phase 5 — Connected understanding
**Production behavior:** unchanged

## Corrected result

The first candidate failed final review. Its 66 names repeated one unchanged
fixture; five models, Model C selection, classification, recommendation, and
next task were reported independently of executed model findings. It also
accepted unresolved source-binding closure, synthesized an upstream Explanation
revision, collapsed source and Evidence scope, and under-validated records.

The first corrected oracle still hashed its complete scenario wrapper rather
than the actual producer argument. Case identity, descriptions, expected
assertions, roles, recipients, default Product scope, grant decoys, and
presentation metadata therefore created false input identities.

The accounting-corrected oracle distinguishes six stable digests:

- scenario wrapper: the complete `ScenarioWrapper`, diagnostic only;
- raw producer input: the exact argument passed to
  `produceCanonicalUnderstandingAudienceLineage`;
- normalized producer input: that argument after benchmark-owned canonical
  set and ordering normalization;
- model input: normalized producer input plus exact model identity and policy
  configuration;
- producer output: the produced value or bounded rejection;
- model output: the exact per-model executable result.

Each digest is SHA-256 over the same recursively key-sorted stable
serialization. Producer-input digests exclude wrapper-only metadata.

After collapsing wrapper-only entries into invariants, the oracle executes 20
truthful behavioral scenarios against five executable policies: 100/100 cells,
zero duplicate/missing/unexpected cells, 58 invariant assertions, six
neutrality invariants, and four tampered-output validations. The sole repeated
normalized-input group is the baseline and reversed-order scenario, classified
`NORMALIZATION_EQUIVALENCE`; raw digests differ and normalized digests and
outputs agree. Unjustified repeated-input groups and no-op transformations are
zero.

The transformation vocabulary is `SEMANTIC_INPUT_MUTATION`,
`NORMALIZATION_INVARIANCE`, `EXTERNAL_NEUTRALITY_INVARIANCE`,
`TAMPERED_OUTPUT_VALIDATION`, and
`MULTIPLE_ASSERTIONS_OVER_ONE_SCENARIO`. Role, default Product scope, recipient,
grant-added, grant-removed, and presentation variants are executed invariants,
not behavioral scenarios. Untyped injection of each external value is rejected.
No model passes every mandatory safety invariant. Producer classification is
**F** and the recommendation is **HOLD FOR MULTIPLE PRODUCER GAPS**.

## Audience-requirement semantics

Current contracts provide subject scope on compositions and Explanations,
source scope through source bindings, and Evidence scope through canonical
attribution. None is explicitly owned as a recipient-audience requirement and
no governed derivation rule currently converts it into one. Consequently:

- composition claims: incomplete — audience requirement and claim-owner
  version unresolved;
- Explanation claims: incomplete — audience requirement and target version
  unresolved;
- Evidence references: incomplete — audience requirement unresolved even when
  attribution and source-binding closure are complete.

No complete or conflicting field family is claimed. Evidence body is
intentionally unavailable. Availability is a later disclosure result.

## Explanation version conclusion

`OrganizationalExplanation` has a stable identity field but no explicit
revision. Current production and replay contracts do not state that identity is
an immutable complete version boundary. The corrected lineage therefore uses
`targetVersionBasis: { kind: "unresolved" }`; no locally synthesized Explanation
revision remains.

## Source and Evidence scope

Source scope resolves only through every exact source-binding record referenced
by an attribution. Evidence scope resolves only from the exact Evidence
attribution. Missing, conflicting, malformed, or cross-organization binding
closure fails closed. The arrays are independently resolved and serialized;
their values may currently coincide because canonical Evidence attribution is
derived from source bindings.

## Contract integrity

Version 1 / producer version 2 records include exact organization,
composition, target kind and identity, explicit target-version basis, field
family, separately resolved subject/source/Evidence scopes, unresolved or
explicit audience requirement, support closure, completeness, bounded reasons,
and fixed resource/operation/purpose semantics. Public helpers independently
normalize and reconstruct record identity, revision, and integrity. Validation
rejects unsupported versions, tampering, wrong resource semantics,
cross-organization scopes, invalid completeness/conflict states, duplicate
identities, and noncanonical ordering.

## Model findings

- A fails: composition-scope inheritance creates an ungoverned audience rule
  and nested-detail broadening risk.
- B fails: direct scope has no audience derivation and discards support lineage.
- C fails: exact support is retained, but field-owned audience requirements are
  unresolved.
- D fails: support roles do not establish conjunctive audience authority and
  over-withholding follows.
- E fails: no canonical producer or administrative owner supplies the explicit
  classification.

## Retained Runtime

The canonical read remains unchanged: 2 compositions, 1 completed Explanation,
22 Evidence attributions, 50 derived lineages, 8 raw conditions, and 10 raw
investigations. Existing reference closure projects 7 conditions and 9
investigations. The new lineage is absent and Runtime bytes are unchanged.

## Next governed task

Final review found that the correct next-task value was still assigned as a
constant. Oracle version 4 replaces that assignment with normalized executed
gap findings, a canonical owner-dependency graph, actionable-root evaluation,
strict blocked-field-set dominance, a boundedness decision, and a task
registry. The final report consumes the resulting integrity-digested decision
object; it has no parallel conclusion path.

The graph reports field audience-requirement governance as an actionable root
whose blocked-field set strictly contains every other root's set. Completed
Explanation immutable-version ownership is an independent actionable root for
composition and Explanation claims. Condition scope is another root;
investigation references are downstream of it. Source-binding closure and
source/Evidence scope separation are resolved.

The combined-task evaluator checks semantic owner, mutation owner, production
boundary, lifecycle, persistence boundary, version boundary, validation
boundary, independent review, and bounded-commit scope. Audience governance
and Explanation version ownership fail the shared semantic, mutation,
production, version, and validation checks, so they derive as `separate`.

Dynamic controls prove that resolving audience governance changes the owner
and task; resolving Explanation version alone leaves audience governance
dominant; resolving both changes the result again; same-owner synthetic gaps
combine; distinct owners remain separate; incomparable roots return no owner
and no task; unresolved prerequisites prevent downstream selection; a missing
registry entry fails closed; and no gaps advances to nested-field disclosure.

The original condition/investigation task therefore remains deferred by the
derived graph. The exact registry-resolved next boundary is:

`DISCOVERY CANONICAL ORGANIZATIONAL UNDERSTANDING FIELD AUDIENCE-REQUIREMENT GOVERNANCE CONTRACT 001`

`DISCOVERY CANONICAL COMPLETED EXPLANATION IMMUTABLE VERSION OWNERSHIP CONTRACT
001` remains a separate later task.
