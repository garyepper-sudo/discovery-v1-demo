# Organizational Understanding Gate 2 Permission Revocation Addendum V3

Status: independently accepted bounded permission-aware evidence noninterference result (`A`). This additive benchmark artifact addresses `GAP-D-004` in the existing research-validation phase. It makes no Product, Runtime, persistence, migration, dependency, or architecture change.

Luna independently accepted two candidate-visible exact request files and their rendered transcripts before any Astra call. The repository records only their content-safe SHA-256 freeze references, source counts, model identity, reasoning setting, and a deterministic parser-to-transcript verifier. Candidate-visible bodies, hidden answer material, scoring rubric, private-basis hashes, sealed outputs, and all provider credentials remain outside the repository.

The verifier accepts only execution-time paths supplied by the freeze owner. It requires normalized LF bytes, the receipt hash of each request and transcript, the complete frozen request contract, deterministic re-rendering from the parsed frozen request, six restricted sources, seven expanded sources, and exactly one expanded-only source. It does not reconstruct a request from a fixture or append call identifiers to candidate-visible content.

The permission reconstruction maps current restricted and revoked access to the same restricted frozen packet; a legitimate expanded grant maps to the expanded packet. The call plan is fixed: restricted, expanded, then the exact same restricted request and transcript again. The sealed-output importer accepts exactly three independently hash-sealed JSON objects.

Luna's content-safe post-Astra score report, retained outside the repository, has SHA-256 `fa66716dc3088fde03f9087d7b49d154bb3c206842053228b567058fdce23f97`. It independently accepted the exact freeze, three fresh isolated `gpt-6-astra` ultra-reasoning calls, and zero automatic retries. Calls one and three used the exact same restricted request and transcript. Deterministic selection passed for restricted, expanded, and revoked current access; protected retrieval, inclusion, and citation eligibility after revocation were all zero.

The restricted calls achieved 100% material citation support, zero unsupported claims, a passing restricted answer range, appropriate uncertainty, 100% material semantic equivalence, zero protected semantic residue, and executive usefulness 17/18. The expanded call made a passing decision-relevant update, cited 100% of expanded-only claims to the added source, retained 100% stability for unaffected restricted facts, had zero unsupported claims, and scored 18/18 usefulness. The post-revocation output had zero expanded-only facts, citations, implications, or recommendation residue, zero unsupported claims, and scored 17/18 usefulness.

The original Gate 2 run remains rejected, and Addenda V1 and V2 remain invalid before model invocation. Addendum V3 closes this bounded Gate 2 permission evidence without rewriting that history. Discovery-shaped governed evidence combined with Astra demonstrated mechanism discrimination, decision-sensitive information value, required/declared/observed separation, institutional adaptation, and permission-aware grant/revocation noninterference across independently authored synthetic organizational worlds. Selective belief revision, stale-context resistance, outcome-linked learning, authentic-history benefit, and real-world organizational understanding remain unproven. No live Product, database, browser, identity, access, source, Meeting Pack, application-provider, push, or deployment mutation occurred.
