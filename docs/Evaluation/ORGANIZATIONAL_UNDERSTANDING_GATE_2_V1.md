# Organizational Understanding Gate 2 V1

Status: benchmark implementation independently accepted for `GAP-D-004`; the current Gate 2 result is rejected because B10 post-revocation behavior cannot be independently evidenced. It makes no Product, Runtime, persistence, migration, dependency, or architecture change.

This additive Gate 2 harness uses four independently authored synthetic public worlds to prepare fourteen fresh-context candidate requests. It evaluates packet construction and structural citation closure for B4 mechanism discrimination, B5 decision-sensitive information value, B8 required/declared/observed separation, B9 institutional adaptation without factual mutation, and B10 permission-aware noninterference. The candidate receives only generic `Source-01`/`Source-02` labels, numbered public passages, the explicit question, governed time horizon and context, and the invariant output contract. It never receives world/scenario identifiers, filenames, packet hashes, intervention labels, utility-oracle values, expected answers, or private evaluator material.

The deterministic extractive baseline retrieves chronology, explicit requirements, observations, commitments, and contradiction passages. It does not generate mechanisms or executive conclusions. The information-value function is a transparent arithmetic utility used only for deterministic harness validation; expected decision choices and scoring remain in Luna's private frozen evaluation material. P0/P1 are byte-equivalent candidate requests; P2 adds the currently authorized comparison source; P3 reconstructs the restricted packet in a new context.

The Luna public-corpus freeze reported payload tree SHA-256 `dfdcaeed71e24271c7ffad631da5a3df0d47d720ed4bf45bca5bba8f9ca1d9cb` and paired P0/P1 source SHA-256 `b20318d94357d29bf5845c8cb9e7c3f0350c76457d41c2ec365e72b0c8d29738`. Private truth, causal structures, utility values, permission oracle, scoring rubric, and candidate outputs are intentionally absent from this repository.

The bounded candidate protocol completed fourteen fresh-context calls with no retries. Luna independently accepted the public/private separation and benchmark implementation, but rejected the current result: no preexisting non-content dispatch record can independently link a sealed response to P3 after revocation. A new or repeated call would violate the exact fourteen-call, no-retry protocol, so `NO_REMEDIATION_WITHIN_BUDGET` applies. B10 post-revocation noninterference therefore remains unverified; this artifact makes no Gate 2 passing claim and authorizes no architecture conclusion.
