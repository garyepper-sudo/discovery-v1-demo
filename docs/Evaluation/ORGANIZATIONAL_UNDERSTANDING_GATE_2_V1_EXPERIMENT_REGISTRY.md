# Organizational Understanding Gate 2 V1 Experiment Registry

| Field | Value |
| --- | --- |
| Experiment | `organizational-understanding-gate2-v1` |
| Status | Implementation accepted; current result rejected (`NO_REMEDIATION_WITHIN_BUDGET`) |
| Candidate protocol | `gpt-6-astra`, ultra reasoning, fourteen maximum fresh contexts, retries 0 |
| Worlds | Four independently authored synthetic public worlds: mechanism, decision information value, institutional meaning/context, and permission noninterference |
| Candidate disclosure | Generic source labels and line numbers only; no filenames, answer keys, expected conclusions, hidden causal structures, utility values, intervention names, digests, prior outputs, or repository state |
| Private material | Luna-owned protected task directory only; not read by the repository harness and not committed |
| Output location | Private task-owned filesystem only; raw candidate output is excluded from Git |

The fixed fourteen-call, no-retry protocol completed. Luna independently accepted the benchmark implementation and separation controls, while rejecting the current result because P3 post-revocation behavior cannot be independently evidenced from a preexisting non-content dispatch record. No correction or new candidate call is permitted within the protocol budget. B10 post-revocation noninterference is unverified; no Gate 2 capability or architecture claim is recorded.

Run `npm run experiment:organizational-understanding-gate2-v1` to write exactly fourteen generic requests, extractive baselines, and a public manifest to `DISCOVERY_GATE2_PACKET_OUTPUT_DIR` or `/private/tmp/discovery-gate2-research-packets`. The candidate interface is one fresh request for each object in `candidate-requests.json`; return a JSON object conforming to `gate2-structured-candidate/v1`. Do not send a manifest, internal packet metadata, evaluator material, or any private file to the candidate.

Run `npm run validate:organizational-understanding-gate2-v1` to validate deterministic request construction, packet authorization/withholding, P0/P1 equality, extractive-only baseline construction, information-value arithmetic, structural citation closure, and the exact request count. Luna independently evaluates mechanism discrimination, information value, institutional categories, factual invariance, permissions, grounding, and usefulness from actual outputs.
