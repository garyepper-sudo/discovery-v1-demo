import assert from "node:assert/strict";
import React from "react";
import {renderToStaticMarkup} from "react-dom/server";

(globalThis as typeof globalThis&{React:typeof React;__founderAddContextBoundAction?:unknown;__founderAddContextFormAction?:unknown}).React=React;
async function main(){const original=React.createElement;React.createElement=((type:unknown,props:Record<string,unknown>|null,...children:React.ReactNode[])=>{if(type==="form")globalThis.__founderAddContextFormAction=props?.action;return original(type as never,props,...children);}) as typeof React.createElement;try{const {FounderAddGovernedContextForm}=await import("../../components/product-alpha/meetings/FounderAddGovernedContextForm");const markup=renderToStaticMarkup(<FounderAddGovernedContextForm seriesAddress="opaque-series" currentSourceCount={1} question="Should the launch proceed?"/>);assert.equal(typeof globalThis.__founderAddContextBoundAction,"function");assert.equal(globalThis.__founderAddContextFormAction,globalThis.__founderAddContextBoundAction);assert.match(markup,/<form/);assert.match(markup,/name="intent"/);assert.match(markup,/value="add-governed-context"/);assert.match(markup,/name="files"/);assert.match(markup,/Add context and refresh preparation/);assert.doesNotMatch(markup,/organizationId|participantRef|productQuestionId|occurrenceId|scopeId|preparedWorkId/);console.log("PASS Founder Add Context rendered-form binding checks=9 bound-exported-action=1 domain-fields=intent,files,sourcePurpose authority-fields=0");}finally{React.createElement=original;}}
main().catch(error=>{console.error(error);process.exitCode=1;});
