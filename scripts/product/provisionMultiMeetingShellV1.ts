import {createLeadershipConversationServerComposition} from"../../product/integration/leadershipConversationServerComposition";import{readNorthstarPreparationLineageSeed}from"../../product/simulations/living-organization-sandbox/preparationLineageFixtureProvisioner";import{NORTHSTAR_AUTHORIZED_PREPARE_SOURCES}from"../../product/workflow/leadershipConversation";import{resolveSandboxPersonas,SANDBOX_ORGANIZATION_ID}from"../../lib/access/sandboxMultiUserAccess";
async function main(){const root=process.env.DISCOVERY_NORTHSTAR_PREPARATION_LINEAGE_FIXTURE_ROOT;if(!root)throw new Error("Northstar preparation lineage seed is unavailable.");const seed=await readNorthstarPreparationLineageSeed({fixtureRoot:root,organizationId:SANDBOX_ORGANIZATION_ID,fixtureId:"northstar-preparation-lineage-fixture-v1",provisioningKey:"northstar-preparation-lineage:v1"}),userId=resolveSandboxPersonas().find(value=>value.key==="sandbox-ceo")!.userId,server=createLeadershipConversationServerComposition();for(const meeting of[{title:"Pipeline Review",timeframe:"Weekly",purpose:"Review supported pipeline constraints and decide what needs attention."},{title:"Product Leadership",timeframe:"Every two weeks",purpose:"Align product leadership around the most material supported priorities."}])await server.activateAndPrepare({contractVersion:"1",userId,organizationId:SANDBOX_ORGANIZATION_ID,questionId:seed.productQuestionId,meetingTitle:meeting.title,timeframe:meeting.timeframe,role:"Chief executive",purpose:meeting.purpose,authorizedSourceRefs:NORTHSTAR_AUTHORIZED_PREPARE_SOURCES.map(value=>value.sourceRef),idempotencyKey:`multi-meeting-shell-v1:${meeting.title.toLowerCase().replaceAll(" ","-")}`});console.log(JSON.stringify({result:"PASS",seriesProvisioned:2,totalExpected:3,writes:"explicit provisioning only"}))}void main();
