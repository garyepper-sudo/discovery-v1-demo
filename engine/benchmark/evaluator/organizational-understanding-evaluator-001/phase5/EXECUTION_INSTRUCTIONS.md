# Phase 5 Execution Instructions

1. Run `npm run validate:organizational-understanding-evaluator-phase-5` before
   recruiting or opening responses.
2. Preserve `generated/STUDY_PLAN.json` and every packet hash unchanged.
3. Give reviewers only `REVIEWER_PROTOCOL.md`, the frozen rubric, and their
   assigned training, qualification, or blinded study packets.
4. Keep `SYNTHETIC_TRANSPORT_ANSWER_KEY.json` inaccessible to reviewers. It is
   for transport validation only and contains no human-study evidence.
5. Freeze each independent response before any reviewer communication.
6. Import genuine responses with explicit `genuine-human` provenance and
   verifiable blinded reviewer metadata. Never relabel synthetic data.
7. Preserve invalid responses in the invalid-response log without admitting
   them as evidence.
8. Run the preregistered analysis without changing thresholds. If the sample is
   too small, classify it as inconclusive.
9. Admit a gold item only after packet integrity, independence, blinding,
   confidence, resolution, and audit-lineage checks pass.
10. External Comparative Validation 002 remains unauthorized regardless of
    study readiness.

