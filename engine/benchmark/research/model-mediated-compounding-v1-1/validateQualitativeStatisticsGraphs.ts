import assert from "node:assert/strict";import {analyzePilot,exploratoryGraph,type PilotRow} from "./pilotAnalysis";import {EXPLORATORY_DISCLAIMER,CONFIRMATORY_STATEMENT} from "./pilotDesign";
const variants=["A|100","B|100","C|100","D|100","C|95","C|90","C|80","C|stale-biased"],rows:PilotRow[]=Array.from({length:24},(_,block)=>variants.map((variant,index)=>({block:`block-${block}`,variant,scenario:`scenario-${block%3}`,cycle:block%8+1,seed:block%5,budget:block%2?2048:4096,score:(block+index)%11/10,inputTokens:100,outputTokens:50,costMicros:10,state:"completed",resolvedReferences:1,totalReferences:1,unsupportedReferences:0,chronologyErrors:0,unsupportedAssertions:0,reviewerA:"pass",reviewerB:"pass"}))).flat();
const first=analyzePilot(rows,192),second=analyzePilot(structuredClone(rows),192);assert.ok(["harmful signal","scenario-dependent signal","promising signal","flat signal","inconclusive pilot","insufficiently sensitive assay"].includes(first.classification));assert.deepEqual(first,second);const graph=exploratoryGraph("pilot",first.comparisons.map(x=>x.estimate));assert.ok(graph.includes(EXPLORATORY_DISCLAIMER)&&first.confirmatoryResult===CONFIRMATORY_STATEMENT);console.log(JSON.stringify({validation:"model-mediated-v1.1-qualitative-statistics-graphs",result:"PASS",checks:3,realProviderCalls:0}));
