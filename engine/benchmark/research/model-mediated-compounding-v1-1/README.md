# Model-Mediated Compounding Benchmark V1.1

V1.1 is a new execution version. It does not mutate or resume the preserved failed V1 preflight.

The only model-output contract changes are provider-schema repairs: every fixed or enumerated scalar is explicitly typed, the output version and schema name are versioned to V1.1, and the parser enforces the existing four-item `discriminatesBetween` limit. Semantic source content, grader relationships, hypotheses, endpoints, scoring, and favorable thresholds are unchanged.

V1.1 stopped permanently after its first canary returned one provider error: `gpt-5.6-sol` rejected the `temperature` parameter before the structured-output schema was evaluated. Pilot requests completed: 0; remaining pilot requests: 191. The exploratory result is **NOT EXECUTED**, the confirmatory result is **NOT TESTED**, and no scientific conclusion exists. V1.1 is not active or resumable; any future continuation requires a separately authorized V1.2 with a new model-parameter contract, preregistration, schedule, archive identity, and request identities.

The exact projected pilot reservation is 4,679,084 integer microdollars and must remain at or below $5.00 before dispatch. No claim may make provider-derived actual cost plus conservative outstanding and remaining reservations exceed the absolute $6.00 ceiling. Concurrency is at most four.

Complete provider response-body bytes remain the evidence source of truth. V1.1 response identity, model, status, output indexes, usage, cost, extracted digest, and parsed digest derive from and bind to the complete envelope. V1 and V1.1 archives use different preregistration, schedule, request, and archive identities.

Strict chained manifests seal the exact 1-, 16-, and 192-attempt states. They reject missing, orphaned, duplicated, cross-version, digest-mismatched, incorrectly bound, unsafe-mode, symlinked, and cardinality-invalid evidence. Exploratory statistics and graphs are created only after the 192-stage seal and can never authorize further dispatch.
