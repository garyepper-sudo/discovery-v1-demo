# Model-Mediated Compounding Benchmark V1

`SYNTHETIC MODEL-MEDIATED BENCHMARK — NOT CUSTOMER OR PRODUCTION EVIDENCE`

This research-only GAP-D-005 harness derives a neutral strict-output execution schedule from the frozen 1,920 structural packets. It adds no Product owner, persistence, route, provider path, or runtime write. `preregistration.ts` freezes endpoints and classifications; `contracts.ts` freezes provider and schema settings; `harness.ts` owns opaque packet projection, blocked scheduling, durable idempotent receipts, strict parsing, and archive verification; `scoring.ts` performs deterministic truth-ledger scoring; and `graphs.ts` creates seven labelled SVGs.

Full raw responses belong in a permission-restricted, content-addressed non-Git archive. Canonical Git retains source, compact manifests, results, and graphs only. The live execution command requires an explicit external archive root and transport controller; validation uses a fake transport and makes zero network or provider requests.

The benchmark can establish only synthetic model-mediated findings for one frozen model and packet set. Authentic user value and organizational outcomes remain **NOT TESTED**.
