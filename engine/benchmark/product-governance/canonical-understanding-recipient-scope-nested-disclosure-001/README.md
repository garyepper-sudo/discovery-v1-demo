# Canonical Understanding Recipient-Scope Benchmark 001

This benchmark-only package addresses the open material multi-role differentiation boundary following `GAP-MR-004`–`GAP-MR-006` in the Discovery 2 production-integration phase. It changes no architecture or production contract and reuses existing canonical disclosure, projection, Runtime, lineage, and Product Communication owners.

Run `npx tsx engine/benchmark/product-governance/canonical-understanding-recipient-scope-nested-disclosure-001/runBenchmark.ts <retained-runtime-file>`. The explicit diagnostic input is read-only and its path is never emitted. The runner verifies Runtime byte identity, executes every case/model/channel cell, derives gaps, selection, classification, recommendation, and next task from evaluated output, then emits deterministic `results.json` and `REPORT.md`. It never writes Runtime, exposes Evidence bodies, uses credentials, or invokes connectors.
