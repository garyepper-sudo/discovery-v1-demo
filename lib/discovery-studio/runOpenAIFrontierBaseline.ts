import { chmod,lstat,mkdir,open,readFile,readdir,rename,rm } from "node:fs/promises";
import { dirname,join,resolve } from "node:path";
import { buildFrontierPrompt,FRONTIER_EXECUTIVE_SCHEMA_ID,FRONTIER_STRUCTURED_SCHEMA_ID } from "./buildFrontierModelPrompt";
import { assertBlindReview,assertFrontierOutput,assertFrontierOutputSchemaAndCitations,assertFrontierRun,assertFrontierRunSemanticRejection,assertFrontierScorecard,assertFrontierSourcePacket,evaluateFrontierCitationFindings,evaluateFrontierSemanticFindings,frontierDigest,issueFrontierRunId,validateFrontierSemantics,type BlindReviewV1,type CrossSystemComparisonV1,type DiscoveryStudioFrontierRunPlanV1,type DiscoveryStudioFrontierSourcePacketV1,type FrontierExecutiveOutputV1,type FrontierHumanScorecardV1,type FrontierProviderUsageV1,type FrontierRunV1,type FrontierStructuredOutputV1,type FrontierSystem,type FrontierTerminalCategoryV1,type FrontierTerminalMetadataV1 } from "./frontierModelContracts";

type Transport=(input:{url:string;headers:Record<string,string>;body:Record<string,unknown>;signal:AbortSignal;recordTransportEntry():Promise<void>})=>Promise<{status:number;json():Promise<unknown>}>;
export const FRONTIER_REQUEST_STAGES=["claimed","request-validated","request-serialized","transport-entry-recorded","provider-response-received","provider-response-body-read","provider-response-parsed","structured-result-validated","semantic-result-validated","citation-result-validated","terminal-result-persisted"] as const;
export type FrontierRequestStageV2=typeof FRONTIER_REQUEST_STAGES[number];
export type FrontierRequestTransitionV2={contractVersion:"2";runId:string;planId:string;stage:FrontierRequestStageV2;sourcePacketDigest:string;requestBodyDigest:string|null;model:string;promptIdentity:string;schemaIdentity:string;configurationDigest:string;httpStatusCategory:string|null;responsesStatus:string|null;providerRequestIdDigest:string|null;providerUsage:FrontierProviderUsageV1|null;priorStateDigest:string|null;transitionDigest:string};
export type FrontierRequestStateHistoryV2={contractVersion:"2";runId:string;planId:string;transitions:FrontierRequestTransitionV2[];terminalDisposition:"active"|"pre-dispatch-failure"|"request-status-unknown"|"provider-response-failure"|"terminal";historyDigest:string};
export function assertFrontierRequestStateHistory(value:unknown):asserts value is FrontierRequestStateHistoryV2{if(!value||typeof value!=="object"||Array.isArray(value))throw new Error("Frontier request history is invalid.");const x=value as FrontierRequestStateHistoryV2,rootKeys=["contractVersion","runId","planId","transitions","terminalDisposition","historyDigest"];if(Object.keys(x).sort().join(",")!==rootKeys.sort().join(",")||x.contractVersion!=="2"||!safe.test(x.runId)||!safe.test(x.planId)||!Array.isArray(x.transitions)||x.transitions.length<1||!["active","pre-dispatch-failure","request-status-unknown","provider-response-failure","terminal"].includes(x.terminalDisposition))throw new Error("Frontier request history is invalid.");let prior:string|null=null,last=-1;const first=x.transitions[0]!,immutable={sourcePacketDigest:first.sourcePacketDigest,model:first.model,promptIdentity:first.promptIdentity,schemaIdentity:first.schemaIdentity,configurationDigest:first.configurationDigest},stageSet=new Set<FrontierRequestStageV2>();for(const t of x.transitions){const{transitionDigest,...base}=t,index=FRONTIER_REQUEST_STAGES.indexOf(t.stage),keys=["configurationDigest","contractVersion","httpStatusCategory","model","planId","priorStateDigest","promptIdentity","providerRequestIdDigest","providerUsage","requestBodyDigest","responsesStatus","runId","schemaIdentity","sourcePacketDigest","stage","transitionDigest"];if(Object.keys(t).sort().join(",")!==keys.sort().join(",")||t.contractVersion!=="2"||t.runId!==x.runId||t.planId!==x.planId||index<=last||t.priorStateDigest!==prior||frontierDigest(base)!==transitionDigest||t.sourcePacketDigest!==immutable.sourcePacketDigest||t.model!==immutable.model||t.promptIdentity!==immutable.promptIdentity||t.schemaIdentity!==immutable.schemaIdentity||t.configurationDigest!==immutable.configurationDigest||![t.sourcePacketDigest,t.configurationDigest].every(v=>/^[a-f0-9]{64}$/.test(v))||typeof t.model!=="string"||typeof t.promptIdentity!=="string"||typeof t.schemaIdentity!=="string"||t.requestBodyDigest!==null&&!/^[a-f0-9]{64}$/.test(t.requestBodyDigest)||index>=FRONTIER_REQUEST_STAGES.indexOf("request-serialized")&&t.requestBodyDigest===null||t.providerRequestIdDigest!==null&&!/^[a-f0-9]{64}$/.test(t.providerRequestIdDigest)||!["success","rate-limit","client-error","server-error",null].includes(t.httpStatusCategory)||!["completed","incomplete","failed","unknown",null].includes(t.responsesStatus))throw new Error("Frontier request transition is invalid.");if(t.providerUsage!==null){const usage=t.providerUsage;if(Object.keys(usage).sort().join(",")!==["inputTokens","outputTokens","totalTokens"].sort().join(",")||![usage.inputTokens,usage.outputTokens,usage.totalTokens].every(Number.isSafeInteger)||usage.inputTokens<0||usage.outputTokens<0||usage.totalTokens!==usage.inputTokens+usage.outputTokens)throw new Error("Frontier request usage is invalid.")}prior=t.transitionDigest;last=index;stageSet.add(t.stage)}const requires=(stage:FrontierRequestStageV2,dependencies:FrontierRequestStageV2[])=>{if(stageSet.has(stage)&&dependencies.some(value=>!stageSet.has(value)))throw new Error("Frontier request stage dependency is invalid.")};requires("request-validated",["claimed"]);requires("request-serialized",["claimed","request-validated"]);requires("transport-entry-recorded",["claimed","request-validated","request-serialized"]);requires("provider-response-received",["transport-entry-recorded"]);requires("provider-response-body-read",["provider-response-received"]);requires("provider-response-parsed",["provider-response-body-read"]);requires("structured-result-validated",["provider-response-parsed"]);requires("semantic-result-validated",["structured-result-validated"]);requires("citation-result-validated",["structured-result-validated"]);requires("terminal-result-persisted",["claimed"]);const hasEntry=stageSet.has("transport-entry-recorded"),hasResponse=stageSet.has("provider-response-received"),hasTerminal=stageSet.has("terminal-result-persisted"),hasParsed=stageSet.has("provider-response-parsed");if(x.terminalDisposition==="active"&&hasTerminal||x.terminalDisposition==="pre-dispatch-failure"&&(hasEntry||hasResponse)||x.terminalDisposition==="request-status-unknown"&&(!hasEntry||hasResponse)||x.terminalDisposition==="provider-response-failure"&&(!hasEntry||!hasResponse)||x.terminalDisposition==="terminal"&&(!hasTerminal||!hasEntry||!hasResponse||!hasParsed))throw new Error("Frontier request terminal disposition is invalid.");const{historyDigest,...base}=x;if(frontierDigest(base)!==historyDigest)throw new Error("Frontier request history integrity is invalid.")}
export class FrontierProviderTransportFailure extends Error{readonly category="pre-dispatch-provider-transport-failure" as const;constructor(){super("Frontier provider transport unavailable.");this.name="FrontierProviderTransportFailure"}}
type ConfigFixture={baselineId:string;version:string;structuredPromptIdentity:string;executivePromptIdentity:string;sourcePacketSchemaIdentity:string;structuredOutputSchemaIdentity:string;executiveOutputSchemaIdentity:string;toolPolicy:"none";externalKnowledgePolicy:"prohibited";storagePolicy:"store-false";maximumRunCountPerExperiment:number;maximumSourceEvents:number;maximumSourceBytes:number;minimumOutputTokens:number;highReasoningMinimumOutputTokens:number;maximumOutputTokens:number;refusalHandling:"typed-unavailable";incompleteOutputHandling:"typed-unavailable";promptInjectionPolicy:"source-is-untrusted-data";promptConfigDigest:string};
const fixturePath=resolve("fixtures/discovery-studio/openai-frontier-baseline-v1.json"),HIGH_REASONING_MINIMUM_OUTPUT_TOKENS=25000,pending=new Map<string,Promise<FrontierRunV1>>(),safe=/^[a-z0-9][a-z0-9-]{0,127}$/;
const root=()=>{const value=resolve(process.env.DISCOVERY_STUDIO_ROOT||"/private/tmp/discovery-studio");if(!/^\/private\/tmp\/discovery-studio(?:\/|$)/.test(value))throw new Error("Studio root is not task-owned.");return value};
async function fixture():Promise<ConfigFixture>{const value=JSON.parse(await readFile(fixturePath,"utf8")) as ConfigFixture,{promptConfigDigest,...base}=value,actual=frontierDigest(base);if(promptConfigDigest!==actual||base.highReasoningMinimumOutputTokens!==HIGH_REASONING_MINIMUM_OUTPUT_TOKENS)throw new Error("Frontier baseline fixture integrity is invalid.");return{...base,promptConfigDigest:actual}}
function environment(){const model=process.env.DISCOVERY_STUDIO_OPENAI_MODEL?.trim()??"",stability=process.env.DISCOVERY_STUDIO_OPENAI_MODEL_STABILITY?.trim()||"snapshot",reasoning=process.env.DISCOVERY_STUDIO_OPENAI_REASONING?.trim()||"model-default",reasoningSupported=process.env.DISCOVERY_STUDIO_OPENAI_REASONING_SUPPORTED==="1",max=Number(process.env.DISCOVERY_STUDIO_OPENAI_MAX_OUTPUT_TOKENS??"4096"),timeout=Number(process.env.DISCOVERY_STUDIO_OPENAI_TIMEOUT_MS??"120000");if(stability!=="snapshot"&&stability!=="alias")throw new Error("Frontier model stability configuration is invalid.");if(!["model-default","low","medium","high"].includes(reasoning)||!Number.isSafeInteger(max)||!Number.isSafeInteger(timeout)||timeout<1000||timeout>600000)throw new Error("Frontier model configuration is invalid.");if(reasoning!=="model-default"&&!reasoningSupported)throw new Error("Configured model reasoning support is not attested.");if(reasoning==="high"&&max<HIGH_REASONING_MINIMUM_OUTPUT_TOKENS)throw new Error("Frontier configuration unavailable — high reasoning requires at least 25000 max output tokens.");return{apiKey:process.env.OPENAI_API_KEY?.trim()??"",model,stability:stability as "snapshot"|"alias",reasoning:reasoning as "model-default"|"low"|"medium"|"high",reasoningSupported,max,timeout}}
export function openAIFrontierConfigurationStatus(){try{const c=environment();return c.apiKey&&c.model?"OpenAI baseline configured" as const:"OpenAI baseline unavailable" as const}catch{return"OpenAI baseline unavailable" as const}}
function schema(system:Exclude<FrontierSystem,"manual-chatgpt-import"|"canonical-product-baseline"|"discovery-generic-candidate">){const sourceIds={type:"array",items:{type:"string"}},nullableString={type:["string","null"]};if(system==="openai-frontier-structured")return{name:"discovery_studio_frontier_structured_v1",strict:true,schema:{type:"object",additionalProperties:false,required:["schemaVersion","executiveBrief","objects","attentionRecommendation","whatWouldChangeAssessment","caveats"],properties:{schemaVersion:{type:"string",const:"1"},executiveBrief:{type:"string"},objects:{type:"array",items:{type:"object",additionalProperties:false,required:["kind","statement","epistemicCategory","supportCategory","sourceEventIds","supportSummary","alternativeExplanation","uncertainty","owner","dueDate","status"],properties:{kind:{type:"string",enum:["attention-item","observation","assessment","possibility","contradiction","mechanism","belief","decision","non-decision","commitment","open-question","resolved-unknown","new-unknown","recommendation","no-intervention","caveat"]},statement:{type:"string"},epistemicCategory:{type:"string",enum:["explicit","inferred","suspected","unavailable"]},supportCategory:{type:"string",enum:["supported","provisional","insufficient"]},sourceEventIds:sourceIds,supportSummary:{type:"string"},alternativeExplanation:nullableString,uncertainty:nullableString,owner:nullableString,dueDate:nullableString,status:nullableString}}},attentionRecommendation:{type:"string"},whatWouldChangeAssessment:{type:"array",items:{type:"string"}},caveats:{type:"array",items:{type:"string"}}}}};return{name:"discovery_studio_frontier_executive_v1",strict:true,schema:{type:"object",additionalProperties:false,required:["schemaVersion","executiveSynthesis","whatMattersMost","whyItMatters","strongestCompetingExplanation","decisions","commitments","unknowns","contradictions","attentionRecommendation","whatWouldChangeAssessment","sourceReferencedClaims","caveats"],properties:{schemaVersion:{type:"string",const:"1"},executiveSynthesis:{type:"string"},whatMattersMost:{type:"array",items:{type:"string"}},whyItMatters:{type:"array",items:{type:"string"}},strongestCompetingExplanation:{type:"string"},decisions:{type:"array",items:{type:"string"}},commitments:{type:"array",items:{type:"string"}},unknowns:{type:"array",items:{type:"string"}},contradictions:{type:"array",items:{type:"string"}},attentionRecommendation:{type:"string"},whatWouldChangeAssessment:{type:"array",items:{type:"string"}},sourceReferencedClaims:{type:"array",items:{type:"object",additionalProperties:false,required:["claimId","claimKind","statement","epistemicCategory","sourceEventIds","supportSummary","uncertainty","location"],properties:{claimId:{type:"string"},claimKind:{type:"string"},statement:{type:"string"},epistemicCategory:{type:"string",enum:["explicit","inferred","suspected","unavailable"]},sourceEventIds:sourceIds,supportSummary:{type:"string"},uncertainty:nullableString,location:{type:"string"}}}},caveats:{type:"array",items:{type:"string"}}}}}}
async function ensure(path:string){await mkdir(path,{recursive:true,mode:0o700});await chmod(path,0o700)}
async function persist(run:FrontierRunV1){assertFrontierRun(run);const dir=join(root(),"frontier-runs");await ensure(dir);const final=join(dir,`${run.runId}.json`),temp=join(dir,`.${run.runId}.${process.pid}.tmp`),bytes=`${JSON.stringify(run,null,2)}\n`;try{const h=await open(temp,"wx",0o600);try{await h.writeFile(bytes);await h.sync()}finally{await h.close()}await rename(temp,final);const d=await open(dir,"r");try{await d.sync()}finally{await d.close()}}catch(error:any){await rm(temp,{force:true});if(error?.code!=="EEXIST")throw error;const existing=await loadFrontierRun(run.runId);if(existing.runDigest!==run.runDigest)throw new Error("Frontier run identity already exists.")}return run}
export async function loadFrontierRun(runId:string){if(!safe.test(runId))throw new Error("Frontier run identity is invalid.");const path=join(root(),"frontier-runs",`${runId}.json`),s=await lstat(path);if(!s.isFile()||s.isSymbolicLink()||(s.mode&0o777)!==0o600)throw new Error("Frontier artifact is unsafe.");const run=JSON.parse(await readFile(path,"utf8"));assertFrontierRun(run);assertFrontierRunSemanticRejection(run);return run}
export async function listFrontierRuns(){const dir=join(root(),"frontier-runs");try{return await Promise.all((await readdir(dir)).filter(x=>x.startsWith("frontier-")&&x.endsWith(".json")).sort().map(x=>loadFrontierRun(x.slice(0,-5))))}catch(error:any){if(error?.code==="ENOENT")return[];throw error}}
async function persistEvaluation(kind:"frontier-comparisons"|"blind-reviews"|"frontier-scorecards",id:string,value:CrossSystemComparisonV1|BlindReviewV1|FrontierHumanScorecardV1){const dir=join(root(),kind);await ensure(dir);const path=join(dir,`${id}.json`),bytes=`${JSON.stringify(value,null,2)}\n`,temp=join(dir,`.${id}.${process.pid}.tmp`);const h=await open(temp,"wx",0o600);try{await h.writeFile(bytes);await h.sync()}finally{await h.close()}await rename(temp,path);return value}
export const saveFrontierComparison=(value:CrossSystemComparisonV1)=>persistEvaluation("frontier-comparisons",value.comparisonId,value);
export const saveBlindReview=(value:BlindReviewV1)=>persistEvaluation("blind-reviews",value.blindReviewId,value);
export async function loadBlindReview(id:string){if(!/^blind-[a-f0-9]{24}$/.test(id))throw new Error("Blind review identity is invalid.");const value=JSON.parse(await readFile(join(root(),"blind-reviews",`${id}.json`),"utf8"));assertBlindReview(value);return value}
export async function listBlindReviews(){const dir=join(root(),"blind-reviews");try{return await Promise.all((await readdir(dir)).filter(name=>/^blind-[a-f0-9]{24}\.json$/.test(name)).sort().map(name=>loadBlindReview(name.slice(0,-5))))}catch(error:any){if(error?.code==="ENOENT")return[];throw error}}
export const saveFrontierScorecard=async(value:FrontierHumanScorecardV1)=>{assertFrontierScorecard(value);await persistEvaluation("frontier-scorecards",value.scorecardId,value);return value};
function metadata(input:Omit<FrontierTerminalMetadataV1,"contractVersion"|"metadataDigest">):FrontierTerminalMetadataV1{const base={contractVersion:"1" as const,...input};return{...base,metadataDigest:frontierDigest(base)}}
function usageFrom(raw:any):FrontierProviderUsageV1|null{if(!raw?.usage||![raw.usage.input_tokens,raw.usage.output_tokens,raw.usage.total_tokens].every(Number.isSafeInteger))return null;return{inputTokens:raw.usage.input_tokens,outputTokens:raw.usage.output_tokens,totalTokens:raw.usage.total_tokens}}
function terminal(base:Omit<FrontierRunV1,"terminal"|"output"|"normalizedContentDigest"|"usage"|"latencyMs"|"returnedModel"|"terminalMetadata"|"runDigest">,kind:FrontierRunV1["terminal"],terminalMetadata:FrontierTerminalMetadataV1,usage:FrontierProviderUsageV1|null,latencyMs:number|null,returnedModel:string|null=null):FrontierRunV1{const unsigned={...base,terminal:kind,output:null,normalizedContentDigest:null,usage,latencyMs,returnedModel,terminalMetadata};return{...unsigned,runDigest:frontierDigest(unsigned)}}
function assertOutputShape(value:unknown,system:FrontierSystem){if(!value||typeof value!=="object"||Array.isArray(value))throw new Error("shape");const x=value as Record<string,unknown>;const exact=(keys:string[])=>{if(Object.keys(x).sort().join(",")!==keys.sort().join(","))throw new Error("shape")};if(system==="openai-frontier-structured"){exact(["schemaVersion","executiveBrief","objects","attentionRecommendation","whatWouldChangeAssessment","caveats"]);if(x.schemaVersion!=="1"||typeof x.executiveBrief!=="string"||typeof x.attentionRecommendation!=="string"||!Array.isArray(x.objects)||!Array.isArray(x.whatWouldChangeAssessment)||!Array.isArray(x.caveats))throw new Error("shape");for(const raw of x.objects){if(!raw||typeof raw!=="object"||Array.isArray(raw))throw new Error("shape");const item=raw as Record<string,unknown>,keys=["kind","statement","epistemicCategory","supportCategory","sourceEventIds","supportSummary","alternativeExplanation","uncertainty","owner","dueDate","status"];if(Object.keys(item).sort().join(",")!==keys.sort().join(",")||typeof item.statement!=="string"||typeof item.supportSummary!=="string"||!Array.isArray(item.sourceEventIds))throw new Error("shape")}return}if(system==="openai-frontier-executive"){exact(["schemaVersion","executiveSynthesis","whatMattersMost","whyItMatters","strongestCompetingExplanation","decisions","commitments","unknowns","contradictions","attentionRecommendation","whatWouldChangeAssessment","sourceReferencedClaims","caveats"]);if(x.schemaVersion!=="1"||typeof x.executiveSynthesis!=="string"||!Array.isArray(x.sourceReferencedClaims))throw new Error("shape");return}throw new Error("shape")}
function assertOutputCitations(value:any,system:FrontierSystem,packet:DiscoveryStudioFrontierSourcePacketV1){const allowed=new Set(packet.enabledEvents.map(event=>event.eventId)),claims=system==="openai-frontier-structured"?value.objects:value.sourceReferencedClaims;for(const item of claims)if(!Array.isArray(item.sourceEventIds)||item.sourceEventIds.some((id:unknown)=>typeof id!=="string"||!allowed.has(id)))throw new Error("citation")}
function outputText(raw:any){if(typeof raw?.output_text==="string")return raw.output_text;const values=raw?.output?.flatMap((item:any)=>item?.content??[]).filter((c:any)=>c?.type==="output_text").map((c:any)=>c.text);return Array.isArray(values)&&values.length===1?values[0]:null}
export async function runOpenAIFrontierBaseline(input:{runId?:string;runName:string;system:Exclude<FrontierSystem,"manual-chatgpt-import"|"canonical-product-baseline"|"discovery-generic-candidate">;sourcePacket:DiscoveryStudioFrontierSourcePacketV1;sourceCommit:string;transport?:Transport;testFailurePoint?:"validation-before-serialization"|"serialization"|"history-before-entry"|"response-receipt-history"|"terminal-history";stateObserver?:(stage:FrontierRequestStageV2,data:{requestBodyDigest?:string;status?:number;responsesStatus?:string;providerRequestId?:string;providerUsage?:FrontierProviderUsageV1|null})=>Promise<void>}){
 const runId=input.runId??issueFrontierRunId();if(pending.has(runId))return pending.get(runId)!;try{return await loadFrontierRun(runId)}catch(error:any){if(error?.code!=="ENOENT"){} }
 const job=(async()=>{assertFrontierSourcePacket(input.sourcePacket);const f=await fixture(),c=environment(),prompt=buildFrontierPrompt(input.system,input.sourcePacket),schemaIdentity=input.system==="openai-frontier-structured"?FRONTIER_STRUCTURED_SCHEMA_ID:FRONTIER_EXECUTIVE_SCHEMA_ID;
  if(c.reasoning==="high"&&c.max<f.highReasoningMinimumOutputTokens)throw new Error("Frontier configuration unavailable — high reasoning requires at least 25000 max output tokens.");
  const base={contractVersion:"1" as const,runId,runName:input.runName,system:input.system,sourcePacket:input.sourcePacket,sourceCommit:input.sourceCommit,requestedModel:c.model||null,modelStability:c.stability,reasoning:c.reasoning,promptIdentity:prompt.promptIdentity,configurationDigest:frontierDigest({fixture:f.promptConfigDigest,model:c.model,stability:c.stability,reasoning:c.reasoning,reasoningSupported:c.reasoningSupported,max:c.max,timeout:c.timeout}),outputSchemaIdentity:schemaIdentity,attestation:null,warning:c.stability==="alias"?"UNFROZEN MODEL ALIAS — NOT A DURABLE BENCHMARK":null};const observe=input.stateObserver??(async()=>{});await observe("request-validated",{});if(input.testFailurePoint&&process.env.NODE_ENV!=="test")throw new Error("Frontier test seam is unavailable.");if(input.testFailurePoint==="validation-before-serialization")throw new Error("Injected validation boundary failure.");
  const meta=(category:FrontierTerminalCategoryV1,patch:Partial<Omit<FrontierTerminalMetadataV1,"contractVersion"|"category"|"requestedModel"|"effectiveTimeoutMs"|"effectiveMaxOutputTokens"|"promptIdentity"|"schemaIdentity"|"sourcePacketDigest"|"metadataDigest">>={})=>metadata({category,httpStatusCategory:"transport-unavailable",responsesStatus:"unknown",incompleteReason:null,refusalPresent:false,requestedModel:c.model||null,returnedModel:null,effectiveTimeoutMs:c.timeout,effectiveMaxOutputTokens:c.max,promptIdentity:prompt.promptIdentity,schemaIdentity,sourcePacketDigest:input.sourcePacket.packetDigest,providerUsage:null,latencyMs:null,structuredOutputStatus:"not-evaluated",semanticValidationStatus:"not-evaluated",citationValidationStatus:"not-evaluated",...patch});
  if(!c.apiKey||!c.model)return persist(terminal(base,"unavailable-missing-configuration",meta("terminal-unavailable-other"),null,null));const bytes=Buffer.byteLength(JSON.stringify(input.sourcePacket));if(input.sourcePacket.enabledEvents.length>f.maximumSourceEvents||bytes>f.maximumSourceBytes||c.max<f.minimumOutputTokens||c.max>f.maximumOutputTokens)return persist(terminal(base,"unavailable-source-limit",meta("terminal-unavailable-other"),null,null));
  const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),c.timeout),started=Date.now(),body:any={model:c.model,store:false,input:[{role:"developer",content:[{type:"input_text",text:prompt.instructions}]},{role:"user",content:[{type:"input_text",text:prompt.input}]}],text:{format:{type:"json_schema",...schema(input.system)}},max_output_tokens:c.max};if(c.reasoning!=="model-default"&&c.reasoningSupported)body.reasoning={effort:c.reasoning};if(input.testFailurePoint==="serialization")throw new Error("Injected serialization failure.");const requestBodyDigest=frontierDigest(body);await observe("request-serialized",{requestBodyDigest});let responseReceived=false,transportEntered=false;
  try{const transport=input.transport??(async value=>{await value.recordTransportEntry();const r=await fetch(value.url,{method:"POST",headers:value.headers,body:JSON.stringify(value.body),signal:value.signal});return{status:r.status,json:()=>r.json()}}),response=await transport({url:"https://api.openai.com/v1/responses",headers:{Authorization:`Bearer ${c.apiKey}`,"Content-Type":"application/json"},body,signal:controller.signal,recordTransportEntry:async()=>{await observe("transport-entry-recorded",{requestBodyDigest});transportEntered=true}}),latencyMs=Date.now()-started;responseReceived=true;await observe("provider-response-received",{requestBodyDigest,status:response.status});let raw:any=null;try{raw=await response.json()}catch{}if(raw!==null){await observe("provider-response-body-read",{requestBodyDigest,status:response.status});await observe("provider-response-parsed",{requestBodyDigest,status:response.status,responsesStatus:typeof raw?.status==="string"?raw.status:"unknown",providerRequestId:typeof raw?.id==="string"?raw.id:undefined,providerUsage:usageFrom(raw)})}const providerUsage=usageFrom(raw),returnedModel=typeof raw?.model==="string"?raw.model:null,httpStatusCategory=response.status===429?"rate-limit":response.status>=500?"server-error":response.status>=400?"client-error":"success",responsesStatus=["completed","incomplete","failed"].includes(raw?.status)?raw.status:"unknown";
   const measured=(category:FrontierTerminalCategoryV1,patch:Partial<FrontierTerminalMetadataV1>={})=>meta(category,{httpStatusCategory,responsesStatus,incompleteReason:null,refusalPresent:false,returnedModel,providerUsage,latencyMs,...patch});
   if(response.status<200||response.status>=300)return persist(terminal(base,response.status===429?"unavailable-rate-limit":"unavailable-transport",measured("terminal-provider-http-failure"),providerUsage,latencyMs,returnedModel));
   if(returnedModel&&returnedModel!==c.model)return persist(terminal(base,"unavailable-model-mismatch",measured("terminal-model-substitution"),providerUsage,latencyMs,returnedModel));
   if(raw?.status==="incomplete"){const reason=raw?.incomplete_details?.reason==="max_output_tokens"?"max_output_tokens":"other";return persist(terminal(base,"unavailable-incomplete",measured(reason==="max_output_tokens"?"terminal-incomplete-max-output-tokens":"terminal-unavailable-other",{incompleteReason:reason,structuredOutputStatus:outputText(raw)?"invalid":"absent"}),providerUsage,latencyMs,returnedModel))}
   const refused=Boolean(raw?.output?.some((item:any)=>item?.content?.some((content:any)=>content?.type==="refusal")));if(refused)return persist(terminal(base,"unavailable-refusal",measured("terminal-refusal",{refusalPresent:true,structuredOutputStatus:"absent"}),providerUsage,latencyMs,returnedModel));
   if(raw?.status==="failed")return persist(terminal(base,"unavailable-transport",measured("terminal-provider-transport-failure"),providerUsage,latencyMs,returnedModel));const output=outputText(raw);if(!output)return persist(terminal(base,"unavailable-malformed",measured("terminal-structured-output-invalid",{structuredOutputStatus:"absent"}),providerUsage,latencyMs,returnedModel));
   let value:unknown;try{value=JSON.parse(output);assertOutputShape(value,input.system)}catch{return persist(terminal(base,"unavailable-malformed",measured("terminal-structured-output-invalid",{structuredOutputStatus:"invalid"}),providerUsage,latencyMs,returnedModel))}
   try{assertOutputCitations(value,input.system,input.sourcePacket)}catch{const findings=evaluateFrontierCitationFindings(value as FrontierStructuredOutputV1|FrontierExecutiveOutputV1,input.system,input.sourcePacket),rejectionBase={contractVersion:"1" as const,disposition:"REJECTED FRONTIER OUTPUT — NOT BENCHMARK ELIGIBLE" as const,output:value as FrontierStructuredOutputV1|FrontierExecutiveOutputV1,outputDigest:frontierDigest(value),findings,failedFindingCount:findings.length,findingAggregateDigest:frontierDigest(findings.map(finding=>finding.findingDigest))},semanticRejection={...rejectionBase,rejectionDigest:frontierDigest(rejectionBase)},terminalMetadata=measured("terminal-citation-validation-failed",{structuredOutputStatus:"valid",citationValidationStatus:"FAIL"}),unsigned={...base,returnedModel,terminal:"unavailable-malformed" as const,output:null,normalizedContentDigest:null,usage:providerUsage,latencyMs,terminalMetadata,semanticRejection};return persist({...unsigned,runDigest:frontierDigest(unsigned)})}
   try{assertFrontierOutputSchemaAndCitations(value,input.system,input.sourcePacket)}catch{return persist(terminal(base,"unavailable-malformed",measured("terminal-structured-output-invalid",{structuredOutputStatus:"invalid"}),providerUsage,latencyMs,returnedModel))}const findings=evaluateFrontierSemanticFindings(value as FrontierStructuredOutputV1|FrontierExecutiveOutputV1,input.system,input.sourcePacket),failed=findings.filter(finding=>finding.result==="fail");if(failed.length){const rejectionBase={contractVersion:"1" as const,disposition:"REJECTED FRONTIER OUTPUT — NOT BENCHMARK ELIGIBLE" as const,output:value as FrontierStructuredOutputV1|FrontierExecutiveOutputV1,outputDigest:frontierDigest(value),findings,failedFindingCount:failed.length,findingAggregateDigest:frontierDigest(findings.map(finding=>finding.findingDigest))},semanticRejection={...rejectionBase,rejectionDigest:frontierDigest(rejectionBase)},terminalMetadata=measured("terminal-semantic-validation-failed",{structuredOutputStatus:"valid",semanticValidationStatus:"FAIL",citationValidationStatus:"PASS"}),unsigned={...base,returnedModel,terminal:"unavailable-malformed" as const,output:null,normalizedContentDigest:null,usage:providerUsage,latencyMs,terminalMetadata,semanticRejection};return persist({...unsigned,runDigest:frontierDigest(unsigned)})}
   try{assertFrontierOutput(value,input.system,input.sourcePacket)}catch{return persist(terminal(base,"unavailable-malformed",measured("terminal-semantic-validation-failed",{structuredOutputStatus:"valid",semanticValidationStatus:"FAIL",citationValidationStatus:"PASS"}),providerUsage,latencyMs,returnedModel))}
   try{validateFrontierSemantics(value,input.system,input.sourcePacket)}catch{return persist(terminal(base,"unavailable-malformed",measured("terminal-citation-validation-failed",{structuredOutputStatus:"valid",semanticValidationStatus:"PASS",citationValidationStatus:"FAIL"}),providerUsage,latencyMs,returnedModel))}
   const terminalMetadata=measured("terminal-success",{structuredOutputStatus:"valid",semanticValidationStatus:"PASS",citationValidationStatus:"PASS"}),unsigned={...base,returnedModel,terminal:"complete" as const,output:value as any,normalizedContentDigest:frontierDigest(value),usage:providerUsage,latencyMs,terminalMetadata};return persist({...unsigned,runDigest:frontierDigest(unsigned)})
  }catch(error){const latencyMs=Date.now()-started;if((error as Error).name==="AbortError")return persist(terminal(base,"unavailable-timeout",meta("terminal-provider-timeout",{latencyMs}),null,latencyMs));if(error instanceof FrontierProviderTransportFailure||responseReceived)return persist(terminal(base,"unavailable-transport",meta("terminal-provider-transport-failure",{latencyMs}),null,latencyMs));return persist(terminal(base,"unavailable-transport",meta(transportEntered?"terminal-request-status-unknown":"terminal-pre-dispatch-infrastructure-failure",{latencyMs}),null,latencyMs))}finally{clearTimeout(timer)}})();pending.set(runId,job);try{return await job}finally{pending.delete(runId)}
}
export async function importManualChatGPT(input:{runName:string;sourcePacket:DiscoveryStudioFrontierSourcePacketV1;sourceCommit:string;prose:string;attestation:{newBlankConversation:boolean;exactSourceOnlyPacket:boolean;noDiscoveryResult:boolean;noPriorScenarioAnalysis:boolean;browsingDisabled:boolean;toolsDisabled:boolean;copiedWithoutSubstantiveEditing:boolean;modelLabel?:string}}){assertFrontierSourcePacket(input.sourcePacket);if(!input.prose.trim()||input.prose.length>100000)throw new Error("Manual ChatGPT output is invalid.");const attestation={...input.attestation,modelLabel:input.attestation.modelLabel?.trim()||null,outputFormat:"prose" as const},base={contractVersion:"1" as const,runId:issueFrontierRunId(),runName:input.runName,system:"manual-chatgpt-import" as const,sourcePacket:input.sourcePacket,sourceCommit:input.sourceCommit,requestedModel:null,returnedModel:null,modelStability:"manual-attested" as const,reasoning:null,promptIdentity:"manual-chatgpt-source-only-v1",configurationDigest:frontierDigest({manual:true}),outputSchemaIdentity:"external-plain-text-v1",terminal:"complete" as const,output:input.prose,normalizedContentDigest:frontierDigest(input.prose),usage:null,latencyMs:null,attestation,warning:"User-supplied ChatGPT product output — not API-reproducible"};return persist({...base,runDigest:frontierDigest(base)})}
export async function importManualChatGPTStructured(input:{runName:string;sourcePacket:DiscoveryStudioFrontierSourcePacketV1;sourceCommit:string;json:string;schema:"structured"|"executive";attestation:{newBlankConversation:boolean;exactSourceOnlyPacket:boolean;noDiscoveryResult:boolean;noPriorScenarioAnalysis:boolean;browsingDisabled:boolean;toolsDisabled:boolean;copiedWithoutSubstantiveEditing:boolean;modelLabel?:string}}){const wrapper=JSON.parse(input.json);if(!wrapper||typeof wrapper!=="object"||Array.isArray(wrapper)||Object.keys(wrapper).sort().join(",")!=="output,sourcePacketDigest"||wrapper.sourcePacketDigest!==input.sourcePacket.packetDigest)throw new Error("Manual structured output source-packet binding is invalid.");const output=wrapper.output,system=input.schema==="structured"?"openai-frontier-structured" as const:"openai-frontier-executive" as const;assertFrontierOutput(output,system,input.sourcePacket);const attestation={...input.attestation,modelLabel:input.attestation.modelLabel?.trim()||null,outputFormat:"structured-json" as const},base={contractVersion:"1" as const,runId:issueFrontierRunId(),runName:input.runName,system:"manual-chatgpt-import" as const,sourcePacket:input.sourcePacket,sourceCommit:input.sourceCommit,requestedModel:null,returnedModel:null,modelStability:"manual-attested" as const,reasoning:null,promptIdentity:"manual-chatgpt-source-only-v2",configurationDigest:frontierDigest({manual:true,schema:input.schema,sourcePacketDigest:input.sourcePacket.packetDigest}),outputSchemaIdentity:input.schema==="structured"?FRONTIER_STRUCTURED_SCHEMA_ID:FRONTIER_EXECUTIVE_SCHEMA_ID,terminal:"complete" as const,output,normalizedContentDigest:frontierDigest(output),usage:null,latencyMs:null,attestation,warning:"User-supplied structured ChatGPT product output — not API verified"};return persist({...base,runDigest:frontierDigest(base)})}

export type FrontierExecutionClaimV1={contractVersion:"1";runId:string;experimentId:string;runName:string;systemMode:string;sourcePacketDigest:string;requestedModel:string;promptIdentity:string;schemaIdentity:string;reasoning:string;requestConfigurationDigest:string;cohortDigest:string;state:"claimed"|"request-started"|"terminal-success"|"terminal-unavailable"|"terminal-blocked"|"request-status-unknown";claimDigest:string};
export function assertFrontierExecutionClaim(value:unknown):asserts value is FrontierExecutionClaimV1{if(!value||typeof value!=="object")throw new Error("Frontier claim is invalid.");const x=value as Record<string,unknown>,keys=["contractVersion","runId","experimentId","runName","systemMode","sourcePacketDigest","requestedModel","promptIdentity","schemaIdentity","reasoning","requestConfigurationDigest","cohortDigest","state","claimDigest"];if(Object.keys(x).sort().join(",")!==keys.sort().join(",")||x.contractVersion!=="1"||!keys.slice(1,-1).every(key=>typeof x[key]==="string")||!["claimed","request-started","terminal-success","terminal-unavailable","terminal-blocked","request-status-unknown"].includes(String(x.state)))throw new Error("Frontier claim is invalid.");const{claimDigest,...unsigned}=x;if(typeof claimDigest!=="string"||frontierDigest(unsigned)!==claimDigest)throw new Error("Frontier claim integrity is invalid.")}
const claimUnsigned=(v:Omit<FrontierExecutionClaimV1,"claimDigest">|FrontierExecutionClaimV1)=>{const{claimDigest:_ignored,...unsigned}=v as FrontierExecutionClaimV1;return unsigned};
async function writeClaim(value:FrontierExecutionClaimV1,exclusive=false){const dir=join(root(),"frontier-claims");await ensure(dir);const final=join(dir,`${value.runId}.json`),temp=join(dir,`.${value.runId}.${process.pid}.${Date.now()}.tmp`),bytes=`${JSON.stringify(value,null,2)}\n`,h=await open(temp,"wx",0o600);try{await h.writeFile(bytes);await h.sync()}finally{await h.close()}try{if(exclusive){const target=await open(final,"wx",0o600);try{await target.writeFile(bytes);await target.sync()}finally{await target.close()}await rm(temp,{force:true})}else await rename(temp,final);const parent=await open(dir,"r");try{await parent.sync()}finally{await parent.close()}if(await readFile(final,"utf8")!==bytes)throw new Error("Frontier claim durable verification failed.")}catch(error){await rm(temp,{force:true});throw error}return value}
export async function loadFrontierClaim(runId:string){const path=join(root(),"frontier-claims",`${runId}.json`),s=await lstat(path);if(!s.isFile()||s.isSymbolicLink()||(s.mode&0o777)!==0o600)throw new Error("Frontier claim is unsafe.");const value=JSON.parse(await readFile(path,"utf8")) as FrontierExecutionClaimV1,{claimDigest,...unsigned}=value;if(frontierDigest(unsigned)!==claimDigest)throw new Error("Frontier claim integrity is invalid.");return value}
type FrontierExecutionLeaseV1={contractVersion:"1";runId:string;planId:string;ownerPid:number;issuedAtMs:number;expiresAtMs:number;leaseDigest:string};
const leasePath=(runId:string)=>join(root(),"frontier-request-leases",`${runId}.json`);
const leaseExpirations=new Map<number,number>();
async function writeExecutionLease(runId:string,planId:string,timeoutMs:number){const dir=join(root(),"frontier-request-leases");await ensure(dir);const issuedAtMs=Date.now(),base={contractVersion:"1" as const,runId,planId,ownerPid:process.pid,issuedAtMs,expiresAtMs:issuedAtMs+timeoutMs+60000},value={...base,leaseDigest:frontierDigest(base)},bytes=`${JSON.stringify(value,null,2)}\n`,final=leasePath(runId),temp=join(dir,`.${runId}.${process.pid}.${Date.now()}.tmp`),h=await open(temp,"wx",0o600);try{await h.writeFile(bytes);await h.sync()}finally{await h.close()}await rename(temp,final);const parent=await open(dir,"r");try{await parent.sync()}finally{await parent.close()}if(await readFile(final,"utf8")!==bytes)throw new Error("Frontier execution lease durable verification failed.");return value}
async function loadExecutionLease(runId:string):Promise<FrontierExecutionLeaseV1|null>{try{const path=leasePath(runId),s=await lstat(path);if(!s.isFile()||s.isSymbolicLink()||(s.mode&0o777)!==0o600)throw new Error("Frontier execution lease is unsafe.");const value=JSON.parse(await readFile(path,"utf8")) as FrontierExecutionLeaseV1,{leaseDigest,...base}=value;if(Object.keys(value).sort().join(",")!==["contractVersion","runId","planId","ownerPid","issuedAtMs","expiresAtMs","leaseDigest"].sort().join(",")||value.contractVersion!=="1"||value.runId!==runId||!safe.test(value.planId)||![value.ownerPid,value.issuedAtMs,value.expiresAtMs].every(Number.isSafeInteger)||value.ownerPid<1||value.issuedAtMs<1||value.expiresAtMs<=value.issuedAtMs||value.expiresAtMs-value.issuedAtMs>660000||frontierDigest(base)!==leaseDigest)throw new Error("Frontier execution lease is invalid.");leaseExpirations.set(value.ownerPid,value.expiresAtMs);return value}catch(error:any){if(error?.code==="ENOENT")return null;throw error}}
function ownerProcessAlive(pid:number){return Date.now()<(leaseExpirations.get(pid)??0)}
async function removeExecutionLease(runId:string){await rm(leasePath(runId),{force:true})}
async function writeRequestHistory(value:FrontierRequestStateHistoryV2){assertFrontierRequestStateHistory(value);const dir=join(root(),"frontier-request-histories");await ensure(dir);const final=join(dir,value.runId+".json"),temp=join(dir,"."+value.runId+"."+process.pid+"."+Date.now()+".tmp"),bytes=JSON.stringify(value,null,2)+"\n",h=await open(temp,"wx",0o600);try{await h.writeFile(bytes);await h.sync()}finally{await h.close()}await rename(temp,final);const parent=await open(dir,"r");try{await parent.sync()}finally{await parent.close()}if(await readFile(final,"utf8")!==bytes)throw new Error("Frontier request history durable verification failed.");return value}
export type FrontierHardGateReconciliationV1={contractVersion:"1";reconciliationId:string;runId:string;planId:string;providerRunDigest:string;hardGateResultDigest:string;priorHistoryDigest:string;correctedHistoryDigest:string;priorPacketId:string;priorPacketDigest:string;disposition:"temporal-hard-gate-reconciled";providerRequestIssued:false;reconciliationDigest:string};
export function assertFrontierHardGateReconciliation(value:unknown):asserts value is FrontierHardGateReconciliationV1{if(!value||typeof value!=="object"||Array.isArray(value))throw new Error("Frontier hard-gate reconciliation is invalid.");const x=value as FrontierHardGateReconciliationV1,keys=["contractVersion","reconciliationId","runId","planId","providerRunDigest","hardGateResultDigest","priorHistoryDigest","correctedHistoryDigest","priorPacketId","priorPacketDigest","disposition","providerRequestIssued","reconciliationDigest"];if(Object.keys(x).sort().join(",")!==keys.sort().join(",")||x.contractVersion!=="1"||!/^reconciliation-[a-f0-9]{24}$/.test(x.reconciliationId)||!safe.test(x.runId)||!safe.test(x.planId)||!safe.test(x.priorPacketId)||![x.providerRunDigest,x.hardGateResultDigest,x.priorHistoryDigest,x.correctedHistoryDigest,x.priorPacketDigest,x.reconciliationDigest].every(value=>/^[a-f0-9]{64}$/.test(value))||x.disposition!=="temporal-hard-gate-reconciled"||x.providerRequestIssued!==false)throw new Error("Frontier hard-gate reconciliation is invalid.");const{reconciliationDigest,...unsigned}=x,{reconciliationId,...identity}=unsigned;if(frontierDigest(unsigned)!==reconciliationDigest||reconciliationId!==`reconciliation-${frontierDigest(identity).slice(0,24)}`)throw new Error("Frontier hard-gate reconciliation integrity is invalid.")}
export async function reconcileFrontierHardGateRequestState(input:{run:FrontierRunV1;planId:string;hardGateResultDigest:string;priorPacketId:string;priorPacketDigest:string}){assertFrontierRun(input.run);assertFrontierRunSemanticRejection(input.run);const category=input.run.terminalMetadata?.category;if(category!=="terminal-semantic-validation-failed"&&category!=="terminal-citation-validation-failed")throw new Error("Frontier run is not an attributable hard-gate result.");const prior=await loadFrontierRequestStateHistory(input.run.runId),stages=prior.transitions.map(item=>item.stage),required=["claimed","request-validated","request-serialized","transport-entry-recorded","provider-response-received","provider-response-body-read","provider-response-parsed","structured-result-validated","semantic-result-validated","citation-result-validated","terminal-result-persisted"];if(prior.planId!==input.planId||prior.terminalDisposition!=="provider-response-failure"||required.some(stage=>!stages.includes(stage as FrontierRequestStageV2))||prior.transitions.at(-1)?.providerUsage?.totalTokens!==input.run.usage?.totalTokens||prior.transitions.at(-1)?.model!==input.run.requestedModel)throw new Error("Frontier hard-gate history cannot be truthfully reconciled.");const{historyDigest:priorHistoryDigest,...historyUnsigned}=prior,correctedBase={...historyUnsigned,terminalDisposition:"terminal" as const},corrected={...correctedBase,historyDigest:frontierDigest(correctedBase)};await writeRequestHistory(corrected);const identity={contractVersion:"1" as const,runId:input.run.runId,planId:input.planId,providerRunDigest:input.run.runDigest,hardGateResultDigest:input.hardGateResultDigest,priorHistoryDigest,correctedHistoryDigest:corrected.historyDigest,priorPacketId:input.priorPacketId,priorPacketDigest:input.priorPacketDigest,disposition:"temporal-hard-gate-reconciled" as const,providerRequestIssued:false as const},base={...identity,reconciliationId:`reconciliation-${frontierDigest(identity).slice(0,24)}`},receipt={...base,reconciliationDigest:frontierDigest(base)};assertFrontierHardGateReconciliation(receipt);const dir=join(root(),"frontier-reconciliations"),final=join(dir,receipt.reconciliationId+".json"),temp=join(dir,"."+receipt.reconciliationId+"."+process.pid+"."+Date.now()+".tmp"),bytes=JSON.stringify(receipt,null,2)+"\n";await ensure(dir);const handle=await open(temp,"wx",0o600);try{await handle.writeFile(bytes);await handle.sync()}finally{await handle.close()}await rename(temp,final);assertFrontierHardGateReconciliation(JSON.parse(await readFile(final,"utf8")));return{history:corrected,receipt}}
export async function loadFrontierHardGateReconciliation(runId:string){if(!safe.test(runId))throw new Error("Frontier reconciliation run identity is invalid.");const dir=join(root(),"frontier-reconciliations");for(const name of await readdir(dir)){if(!name.endsWith(".json"))continue;const value=JSON.parse(await readFile(join(dir,name),"utf8"));assertFrontierHardGateReconciliation(value);if(value.runId===runId)return value}return null}
export type FrontierToolingContinuationV1={contractVersion:"1";continuationId:string;planId:string;priorPlanDigest:string;priorToolingIdentity:string;currentToolingIdentity:string;priorCohortIdentity:string;currentCohortIdentity:string;runName:"northstar-m2-staffing-contradiction-openai-structured-parity-v1";scenarioId:"northstar-controlled-m2-contradiction";disposition:"evaluation-tooling-continuation-authorized";providerRequestIssued:false;continuationDigest:string};
export function assertFrontierToolingContinuation(value:unknown):asserts value is FrontierToolingContinuationV1{if(!value||typeof value!=="object"||Array.isArray(value))throw new Error("Frontier tooling continuation is invalid.");const x=value as FrontierToolingContinuationV1,keys=["contractVersion","continuationId","planId","priorPlanDigest","priorToolingIdentity","currentToolingIdentity","priorCohortIdentity","currentCohortIdentity","runName","scenarioId","disposition","providerRequestIssued","continuationDigest"];if(Object.keys(x).sort().join(",")!==keys.sort().join(",")||x.contractVersion!=="1"||!/^tooling-continuation-[a-f0-9]{24}$/.test(x.continuationId)||!safe.test(x.planId)||![x.priorPlanDigest,x.priorToolingIdentity,x.currentToolingIdentity,x.priorCohortIdentity,x.currentCohortIdentity,x.continuationDigest].every(value=>/^[a-f0-9]{64}$/.test(value))||x.priorToolingIdentity===x.currentToolingIdentity||x.priorCohortIdentity===x.currentCohortIdentity||x.runName!=="northstar-m2-staffing-contradiction-openai-structured-parity-v1"||x.scenarioId!=="northstar-controlled-m2-contradiction"||x.disposition!=="evaluation-tooling-continuation-authorized"||x.providerRequestIssued!==false)throw new Error("Frontier tooling continuation is invalid.");const{continuationDigest,...unsigned}=x,{continuationId,...identity}=unsigned;if(frontierDigest(unsigned)!==continuationDigest||continuationId!==`tooling-continuation-${frontierDigest(identity).slice(0,24)}`)throw new Error("Frontier tooling continuation integrity is invalid.")}
export function assertFrontierToolingContinuationForPlan(value:unknown,plan:DiscoveryStudioFrontierRunPlanV1,currentToolingIdentity:string,currentCohortIdentity:string){assertFrontierToolingContinuation(value);if(value.planId!==plan.planId||value.priorPlanDigest!==plan.planDigest||value.priorToolingIdentity!==plan.evaluationToolingIdentity||value.currentToolingIdentity!==currentToolingIdentity||value.priorCohortIdentity!==plan.cohortIdentity||value.currentCohortIdentity!==currentCohortIdentity||value.runName!==plan.runName||value.scenarioId!==plan.scenarioId)throw new Error("Frontier tooling continuation does not match the frozen plan and current source.")}
export async function saveFrontierToolingContinuation(input:Omit<FrontierToolingContinuationV1,"contractVersion"|"continuationId"|"disposition"|"providerRequestIssued"|"continuationDigest">){const identity={contractVersion:"1" as const,...input,disposition:"evaluation-tooling-continuation-authorized" as const,providerRequestIssued:false as const},base={...identity,continuationId:`tooling-continuation-${frontierDigest(identity).slice(0,24)}`},value={...base,continuationDigest:frontierDigest(base)};assertFrontierToolingContinuation(value);const dir=join(root(),"frontier-tooling-continuations"),final=join(dir,value.planId+".json"),temp=join(dir,"."+value.planId+"."+process.pid+".tmp");await ensure(dir);const handle=await open(temp,"wx",0o600);try{await handle.writeFile(JSON.stringify(value,null,2)+"\n");await handle.sync()}finally{await handle.close()}await rename(temp,final);const loaded=JSON.parse(await readFile(final,"utf8"));assertFrontierToolingContinuation(loaded);return loaded}
export async function loadFrontierToolingContinuation(planId:string){if(!safe.test(planId))throw new Error("Frontier continuation plan identity is invalid.");try{const value=JSON.parse(await readFile(join(root(),"frontier-tooling-continuations",planId+".json"),"utf8"));assertFrontierToolingContinuation(value);return value}catch(error){if((error as NodeJS.ErrnoException).code==="ENOENT")return null;throw error}}
export async function recordFrontierPostResultUiRefresh(input:{runId:string;planId:string;outcome:"succeeded"|"failed"}){if(!safe.test(input.runId)||!safe.test(input.planId))throw new Error("Post-result UI refresh identity is invalid.");const base={contractVersion:"1" as const,runId:input.runId,planId:input.planId,category:input.outcome==="failed"?"post-result-ui-refresh-failed" as const:"post-result-ui-refresh-succeeded" as const,providerStateAffected:false as const},value={...base,eventDigest:frontierDigest(base)},dir=join(root(),"frontier-post-result-events"),final=join(dir,input.runId+".json"),temp=join(dir,"."+input.runId+"."+process.pid+"."+Date.now()+".tmp"),bytes=JSON.stringify(value,null,2)+"\n";await ensure(dir);const handle=await open(temp,"wx",0o600);try{await handle.writeFile(bytes);await handle.sync()}finally{await handle.close()}await rename(temp,final);return value}
export async function finalizeFrontierPostResultUiRefresh(input:{runId:string;planId:string;refresh:()=>void|Promise<void>}){try{await input.refresh();return recordFrontierPostResultUiRefresh({runId:input.runId,planId:input.planId,outcome:"succeeded"})}catch{return recordFrontierPostResultUiRefresh({runId:input.runId,planId:input.planId,outcome:"failed"})}}
export async function loadFrontierRequestStateHistory(runId:string){const path=join(root(),"frontier-request-histories",runId+".json"),s=await lstat(path);if(!s.isFile()||s.isSymbolicLink()||(s.mode&0o777)!==0o600)throw new Error("Frontier request history is unsafe.");const value=JSON.parse(await readFile(path,"utf8"));assertFrontierRequestStateHistory(value);if(value.terminalDisposition==="active"&&!pending.has(runId)){const lease=await loadExecutionLease(runId);if(lease&&ownerProcessAlive(lease.ownerPid))return value;const stages=new Set(value.transitions.map(item=>item.stage)),terminalDisposition=stages.has("provider-response-received")?"provider-response-failure" as const:stages.has("transport-entry-recorded")?"request-status-unknown" as const:"pre-dispatch-failure" as const,{historyDigest:_,...prior}=value,base={...prior,terminalDisposition},reconciled={...base,historyDigest:frontierDigest(base)};await writeRequestHistory(reconciled);try{const claim=await loadFrontierClaim(runId),state=terminalDisposition==="request-status-unknown"?"request-status-unknown" as const:"terminal-unavailable" as const,updated={...claim,state,claimDigest:""},claimed={...updated,claimDigest:frontierDigest(claimUnsigned(updated))};await writeClaim(claimed)}catch(error:any){if(error?.code!=="ENOENT")throw error}await removeExecutionLease(runId);return reconciled}return value}
export async function listFrontierRequestStateHistories(){try{return Promise.all((await readdir(join(root(),"frontier-request-histories"))).filter(name=>name.endsWith(".json")).map(name=>loadFrontierRequestStateHistory(name.slice(0,-5))))}catch(error:any){if(error?.code==="ENOENT")return[];throw error}}
export async function loadFrontierScorecard(id:string){if(!safe.test(id))throw new Error("Frontier scorecard identity is invalid.");const path=join(root(),"frontier-scorecards",`${id}.json`),s=await lstat(path);if(!s.isFile()||s.isSymbolicLink()||(s.mode&0o777)!==0o600)throw new Error("Frontier scorecard is unsafe.");const value=JSON.parse(await readFile(path,"utf8"));assertFrontierScorecard(value);return value}
export async function listFrontierScorecards(){try{return Promise.all((await readdir(join(root(),"frontier-scorecards"))).filter(name=>name.endsWith(".json")).map(name=>loadFrontierScorecard(name.slice(0,-5))))}catch(error:any){if(error?.code==="ENOENT")return[];throw error}}
export async function listFrontierClaims(){try{return Promise.all((await readdir(join(root(),"frontier-claims"))).filter(name=>name.endsWith(".json")).map(name=>loadFrontierClaim(name.slice(0,-5))))}catch(error:any){if(error?.code==="ENOENT")return[];throw error}}
export async function measureFrontierProviderAttemptAccounting(){const [claims,histories,runs]=await Promise.all([listFrontierClaims(),listFrontierRequestStateHistories(),listFrontierRuns()]),plansDirectory=join(root(),"frontier-plans");let plannedRuns=0;try{plannedRuns=(await readdir(plansDirectory)).filter(name=>name.endsWith(".json")).length}catch(error:any){if(error?.code!=="ENOENT")throw error}const localExecutionAttempts=claims.filter(claim=>["request-started","terminal-success","terminal-unavailable","request-status-unknown"].includes(claim.state)).length,preDispatchFailures=histories.filter(history=>history.terminalDisposition==="pre-dispatch-failure").length,measuredEntries=histories.filter(history=>history.transitions.some(item=>item.stage==="transport-entry-recorded")),historicalAttempts=claims.filter(claim=>["request-started","terminal-success","terminal-unavailable","request-status-unknown"].includes(claim.state)&&!histories.some(history=>history.runId===claim.runId)),providerTransportEntries=measuredEntries.length+historicalAttempts.length,providerResponses=histories.filter(history=>history.transitions.some(item=>item.stage==="provider-response-received")).length,acceptedBenchmarkOutputs=claims.filter(claim=>claim.state==="terminal-success").length,requestStatusUnknownAttempts=claims.filter(claim=>claim.state==="request-status-unknown").length,hardGateRuns=runs.filter(run=>run.terminalMetadata?.category==="terminal-semantic-validation-failed"||run.terminalMetadata?.category==="terminal-citation-validation-failed"),hardGateIds=new Set(hardGateRuns.map(run=>run.runId)),infrastructureFailures=claims.filter(claim=>claim.state==="terminal-unavailable"&&!hardGateIds.has(claim.runId)).length,cohortProviderEntries=Object.fromEntries([...new Set(claims.map(claim=>claim.cohortDigest))].sort().map(cohort=>[cohort,measuredEntries.filter(history=>claims.some(claim=>claim.runId===history.runId&&claim.cohortDigest===cohort)).length+historicalAttempts.filter(claim=>claim.cohortDigest===cohort).length]));const base={contractVersion:"1" as const,plannedRuns,localExecutionAttempts,preDispatchFailures,providerTransportEntries,providerResponses,acceptedBenchmarkOutputs,modelHardGateFailures:hardGateRuns.length,infrastructureFailures,requestStatusUnknownAttempts,historicalUnmeasuredProviderEntries:historicalAttempts.length,historicalUnknownProviderEntries:historicalAttempts.filter(claim=>claim.state==="request-status-unknown").length,controlledSetRemaining:Math.max(0,7-providerTransportEntries),cohortProviderEntries};const value={...base,accountingDigest:frontierDigest(base)},dir=join(root(),"frontier-accounting"),final=join(dir,"northstar-frontier-controlled-v1.json"),temp=join(dir,`.accounting.${process.pid}.${Date.now()}.tmp`),bytes=`${JSON.stringify(value,null,2)}\n`;await ensure(dir);const h=await open(temp,"wx",0o600);try{await h.writeFile(bytes);await h.sync()}finally{await h.close()}await rename(temp,final);return value}
export function inspectFrontierRunPlan(input:{experimentId:string;system:Exclude<FrontierSystem,"manual-chatgpt-import"|"canonical-product-baseline"|"discovery-generic-candidate">;sourcePacket:DiscoveryStudioFrontierSourcePacketV1;sourceCommit:string}){assertFrontierSourcePacket(input.sourcePacket);const c=environment(),prompt=buildFrontierPrompt(input.system,input.sourcePacket),schemaIdentity=input.system==="openai-frontier-structured"?FRONTIER_STRUCTURED_SCHEMA_ID:FRONTIER_EXECUTIVE_SCHEMA_ID,requestConfigurationDigest=frontierDigest({sourceCommit:input.sourceCommit,packet:input.sourcePacket.packetDigest,model:c.model,prompt:prompt.promptIdentity,schema:schemaIdentity,reasoning:c.reasoning,reasoningSupported:c.reasoningSupported,max:c.max,timeout:c.timeout}),cohortDigest=frontierDigest({experimentId:input.experimentId,sourceCommit:input.sourceCommit,packet:input.sourcePacket.packetDigest,system:input.system,model:c.model,prompt:prompt.promptIdentity,schema:schemaIdentity,reasoning:c.reasoning});return{requestConfigurationDigest,cohortDigest,promptIdentity:prompt.promptIdentity,schemaIdentity,requestedModel:c.model,reasoning:c.reasoning}}
export function recommendFrontierRunName(input:{claims:FrontierExecutionClaimV1[];currentCohortDigest:string;sourceDigest:string}){input.claims.forEach(assertFrontierExecutionClaim);const consumed=input.claims.filter(claim=>["request-started","terminal-success","terminal-unavailable","request-status-unknown"].includes(claim.state)),current=consumed.filter(claim=>claim.cohortDigest===input.currentCohortDigest),legacyNames=["northstar-m1-openai-structured-a","northstar-m1-openai-structured-b","northstar-m1-openai-structured-c"];if(current.some(claim=>legacyNames.includes(claim.runName))){const next=legacyNames.find(name=>!consumed.some(claim=>claim.runName===name));if(next)return next}let sequence=current.length+1,name=`northstar-m1-openai-structured-new-cohort-${input.sourceDigest.slice(0,12)}-${sequence}`;while(consumed.some(claim=>claim.runName===name)){sequence++;name=`northstar-m1-openai-structured-new-cohort-${input.sourceDigest.slice(0,12)}-${sequence}`}return name}
export async function runClaimedOpenAIFrontierBaseline(input:{experimentId:string;planId?:string;runId:string;runName:string;system:Exclude<FrontierSystem,"manual-chatgpt-import"|"canonical-product-baseline"|"discovery-generic-candidate">;sourcePacket:DiscoveryStudioFrontierSourcePacketV1;sourceCommit:string;transport?:Transport;testFailurePoint?:"validation-before-serialization"|"serialization"|"history-before-entry"|"response-receipt-history"|"terminal-history";}){
 if(process.env.NODE_ENV!=="test"&&input.experimentId==="northstar-frontier-controlled-v1"){const sequence={"northstar-m2-openai-structured-parity-v1-replacement-1":{entries:5,scenarioId:"northstar-controlled-m2"},"northstar-m2-staffing-contradiction-openai-structured-parity-v1":{entries:6,scenarioId:"northstar-controlled-m2-contradiction"}} as const,authorized=sequence[input.runName as keyof typeof sequence];if(!authorized||input.sourcePacket.scenarioId!==authorized.scenarioId)throw new Error("Frontier request is outside the authorized generalization sequence.");const accounting=await measureFrontierProviderAttemptAccounting();if(accounting.providerTransportEntries!==authorized.entries||accounting.controlledSetRemaining<7-authorized.entries)throw new Error("Frontier provider-budget preflight failed.")}
 const c=environment(),plan=inspectFrontierRunPlan(input),planId=input.planId??("frontier-plan-"+frontierDigest({runId:input.runId,experimentId:input.experimentId}).slice(0,24)),{requestConfigurationDigest,cohortDigest,promptIdentity,schemaIdentity}=plan,unsigned=claimUnsigned({contractVersion:"1",runId:input.runId,experimentId:input.experimentId,runName:input.runName,systemMode:input.system,sourcePacketDigest:input.sourcePacket.packetDigest,requestedModel:c.model,promptIdentity,schemaIdentity,reasoning:c.reasoning,requestConfigurationDigest,cohortDigest,state:"claimed"}),claim={...unsigned,claimDigest:frontierDigest(unsigned)};
 try{await writeClaim(claim,true)}catch(error:any){if(error?.code!=="EEXIST")throw error;const existing=await loadFrontierClaim(input.runId);if(existing.requestConfigurationDigest!==requestConfigurationDigest)throw new Error("Frontier run identity has an incompatible claim.");if(existing.state==="request-status-unknown")throw new Error("Frontier request status is not safely retryable.");try{return await loadFrontierRun(input.runId)}catch{throw new Error("Frontier request status is not safely retryable.")}}
 const claimsDir=join(root(),"frontier-claims"),claims=await Promise.all((await readdir(claimsDir)).filter(x=>x.endsWith(".json")).map(x=>loadFrontierClaim(x.slice(0,-5)))),histories=await listFrontierRequestStateHistories(),providerEntries=histories.filter(history=>history.transitions.some(t=>t.stage==="transport-entry-recorded")),historicalPossible=claims.filter(x=>x.experimentId===input.experimentId&&["request-started","terminal-success","terminal-unavailable","request-status-unknown"].includes(x.state)&&!histories.some(history=>history.runId===x.runId));
 if(providerEntries.filter(history=>claims.find(claim=>claim.runId===history.runId)?.cohortDigest===cohortDigest).length+historicalPossible.filter(claim=>claim.cohortDigest===cohortDigest).length>=3||providerEntries.filter(history=>claims.find(claim=>claim.runId===history.runId)?.experimentId===input.experimentId).length+historicalPossible.length>=7){const blocked={...claim,state:"terminal-blocked" as const,claimDigest:""},value={...blocked,claimDigest:frontierDigest(claimUnsigned(blocked))};await writeClaim(value);throw new Error("Frontier controlled provider-entry limit reached.")}
 await writeExecutionLease(input.runId,planId,c.timeout);
 let requestBodyDigest:string|null=null,responseStatus:number|undefined,responsesStatus:string|null=null,providerRequestIdDigest:string|null=null,providerUsage:FrontierProviderUsageV1|null=null,transitions:FrontierRequestTransitionV2[]=[],history:FrontierRequestStateHistoryV2={contractVersion:"2",runId:input.runId,planId,transitions,terminalDisposition:"active",historyDigest:""};
 const storeHistory=async(disposition:FrontierRequestStateHistoryV2["terminalDisposition"]="active")=>{history={...history,transitions:[...transitions],terminalDisposition:disposition,historyDigest:""};const{historyDigest:_,...base}=history;history={...base,historyDigest:frontierDigest(base)};await writeRequestHistory(history)};
 const advance=async(stage:FrontierRequestStageV2,data:{requestBodyDigest?:string;status?:number;responsesStatus?:string;providerRequestId?:string;providerUsage?:FrontierProviderUsageV1|null}={},terminalDisposition:FrontierRequestStateHistoryV2["terminalDisposition"]="active")=>{if((stage==="transport-entry-recorded"&&input.testFailurePoint==="history-before-entry")||(stage==="provider-response-received"&&input.testFailurePoint==="response-receipt-history")||(stage==="terminal-result-persisted"&&input.testFailurePoint==="terminal-history"))throw new Error("Injected request-history persistence failure.");if(data.requestBodyDigest)requestBodyDigest=data.requestBodyDigest;if(data.status!==undefined)responseStatus=data.status;if(data.responsesStatus!==undefined)responsesStatus=data.responsesStatus;if(data.providerRequestId)providerRequestIdDigest=frontierDigest(data.providerRequestId);if(data.providerUsage!==undefined)providerUsage=data.providerUsage;const priorStateDigest=transitions.at(-1)?.transitionDigest??null,httpStatusCategory=responseStatus===undefined?null:responseStatus===429?"rate-limit":responseStatus>=500?"server-error":responseStatus>=400?"client-error":"success",base={contractVersion:"2" as const,runId:input.runId,planId,stage,sourcePacketDigest:input.sourcePacket.packetDigest,requestBodyDigest,model:c.model,promptIdentity,schemaIdentity,configurationDigest:requestConfigurationDigest,httpStatusCategory,responsesStatus,providerRequestIdDigest,providerUsage,priorStateDigest},transition={...base,transitionDigest:frontierDigest(base)};const candidate=[...transitions,transition],candidateBase={contractVersion:"2" as const,runId:input.runId,planId,transitions:candidate,terminalDisposition},candidateHistory={...candidateBase,historyDigest:frontierDigest(candidateBase)};await writeRequestHistory(candidateHistory);transitions=candidate;history=candidateHistory};
 const observedAdvance=async(stage:FrontierRequestStageV2,data:{requestBodyDigest?:string;status?:number;responsesStatus?:string;providerRequestId?:string;providerUsage?:FrontierProviderUsageV1|null}={})=>{try{await advance(stage,data)}catch(error){if(stage!=="provider-response-received"||input.testFailurePoint!=="response-receipt-history")throw error;input.testFailurePoint=undefined;await advance(stage,data);throw error}};
 await advance("claimed");
 const started={...claim,state:"request-started" as const,claimDigest:""},startedValue={...started,claimDigest:frontierDigest(claimUnsigned(started))};await writeClaim(startedValue);
 try{const run=await runOpenAIFrontierBaseline({...input,stateObserver:observedAdvance});if(run.terminalMetadata?.structuredOutputStatus==="valid")await advance("structured-result-validated");if(run.terminalMetadata?.semanticValidationStatus!=="not-evaluated")await advance("semantic-result-validated");if(run.terminalMetadata?.citationValidationStatus!=="not-evaluated")await advance("citation-result-validated");const hasEntry=transitions.some(t=>t.stage==="transport-entry-recorded"),hasResponse=transitions.some(t=>t.stage==="provider-response-received"),hardGate=run.terminalMetadata?.category==="terminal-semantic-validation-failed"||run.terminalMetadata?.category==="terminal-citation-validation-failed",state=run.terminal==="complete"?"terminal-success" as const:run.terminalMetadata?.category==="terminal-request-status-unknown"&&hasEntry?"request-status-unknown" as const:"terminal-unavailable" as const,terminal={...startedValue,state,claimDigest:""},terminalValue={...terminal,claimDigest:frontierDigest(claimUnsigned(terminal))};await writeClaim(terminalValue);const disposition=state==="request-status-unknown"?"request-status-unknown":!hasEntry?"pre-dispatch-failure":hardGate&&hasResponse?"terminal":hasResponse&&run.terminal!=="complete"?"provider-response-failure":"terminal";await advance("terminal-result-persisted",{},disposition);await removeExecutionLease(input.runId);return run}catch(error){const hasEntry=transitions.some(t=>t.stage==="transport-entry-recorded"),hasResponse=transitions.some(t=>t.stage==="provider-response-received"),state=hasEntry&&!hasResponse?"request-status-unknown" as const:"terminal-unavailable" as const,failed={...startedValue,state,claimDigest:""},value={...failed,claimDigest:frontierDigest(claimUnsigned(failed))};await writeClaim(value);await storeHistory(hasEntry&&!hasResponse?"request-status-unknown":hasResponse?"provider-response-failure":"pre-dispatch-failure");await removeExecutionLease(input.runId);throw error}
}
