import "server-only";
import {createHash} from "node:crypto";
import {sourceScopedOutputSchema} from "./buildSourceScopedExecutiveAnalysisRequest";
import type {SourceScopedTransportV1} from "./sourceScopedExecutiveAnalysisContracts";
import type {LifecycleAwareSourceScopedTransportV1,SourceScopedLifecycleHooksV1,SourceScopedTerminalV1} from "../../product/integration/sourceScopedExecutiveAnalysis";
export const SOURCE_SCOPED_RESPONSE_BYTE_LIMIT=8*1024*1024;
class ProviderFailure extends Error{constructor(readonly terminal:SourceScopedTerminalV1){super("Provider unavailable");}}
async function boundedBody(response:Response){if(!response.body)throw new ProviderFailure("malformed-response");const reader=response.body.getReader(),chunks:Uint8Array[]=[];let bytes=0;try{for(;;){const value=await reader.read();if(value.done)break;bytes+=value.value.byteLength;if(bytes>SOURCE_SCOPED_RESPONSE_BYTE_LIMIT){await reader.cancel();throw new ProviderFailure("response-too-large");}chunks.push(value.value);}}finally{reader.releaseLock();}const body=new Uint8Array(bytes);let offset=0;for(const chunk of chunks){body.set(chunk,offset);offset+=chunk.byteLength;}return{body,bytes,digest:createHash("sha256").update(body).digest("hex")};}
export function createOpenAIExecutiveAnalysisTransport(fetchImplementation:typeof globalThis.fetch=globalThis.fetch):LifecycleAwareSourceScopedTransportV1|undefined{const key=process.env.OPENAI_API_KEY,model=process.env.DISCOVERY_ALPHA_ANALYSIS_MODEL;if(!key||!model)return undefined;const build=(hooks?:SourceScopedLifecycleHooksV1):SourceScopedTransportV1=>async({request})=>{const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),request.configuration.timeoutMs),started=Date.now();let terminal:SourceScopedTerminalV1="transport-failed";try{await hooks?.transportEntry();const response=await fetchImplementation("https://api.openai.com/v1/responses",{method:"POST",headers:{authorization:`Bearer ${key}`,"content-type":"application/json"},signal:controller.signal,body:JSON.stringify({model,reasoning:{effort:"high"},max_output_tokens:25000,store:false,tools:[],input:[{role:"developer",content:[{type:"input_text",text:request.prompt}]},{role:"user",content:[{type:"input_text",text:JSON.stringify({question:request.question,sources:request.packet.sources})}]}],text:{format:{type:"json_schema",name:"chief_v1_source_scoped_analysis",strict:true,schema:sourceScopedOutputSchema}}})});terminal=response.ok?"malformed-response":"provider-rejected";await hooks?.responseHeaders(response.headers.get("openai-model"));if(!response.ok)throw new ProviderFailure("provider-rejected");const bounded=await boundedBody(response);await hooks?.responseBody({responseByteCount:bounded.bytes,responseDigest:bounded.digest});let envelope:{model?:unknown;output_text?:unknown;usage?:{input_tokens?:unknown;output_tokens?:unknown;total_tokens?:unknown}};try{envelope=JSON.parse(new TextDecoder().decode(bounded.body)) as typeof envelope;}catch{throw new ProviderFailure("malformed-response");}const returnedModel=typeof envelope.model==="string"?envelope.model:"";if(returnedModel!==model)throw new ProviderFailure("model-mismatch");if(typeof envelope.output_text!=="string")throw new ProviderFailure("malformed-response");let parsed:{sections?:unknown};try{parsed=JSON.parse(envelope.output_text) as {sections?:unknown};}catch{throw new ProviderFailure("malformed-response");}if(!parsed.sections)throw new ProviderFailure("malformed-response");await hooks?.candidateParsed(returnedModel);const integer=(value:unknown)=>Number.isSafeInteger(value)&&Number(value)>=0?Number(value):0,inputTokens=integer(envelope.usage?.input_tokens),outputTokens=integer(envelope.usage?.output_tokens),totalTokens=integer(envelope.usage?.total_tokens);terminal="candidate-validation-failed";return{model:returnedModel,sections:parsed.sections,usage:{inputTokens,outputTokens,totalTokens},latencyMs:Date.now()-started};}catch(error){const category=error instanceof ProviderFailure?error.terminal:terminal;if(hooks)await hooks.terminal(category);throw new Error("Provider unavailable");}finally{clearTimeout(timer);}};const transport=build() as LifecycleAwareSourceScopedTransportV1;transport.withLifecycle=hooks=>build(hooks);return transport;}
